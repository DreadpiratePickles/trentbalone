---
name: Bug report
about: Something Trent did that it should not have, or did not do that it should
title: ""
labels: bug
assignees: ""
---

<!-- A security problem goes through SECURITY.md, not a public issue. Never paste a real key or token. -->

**What happened**
One or two sentences.

**What you expected**

**Exact commands, in order, and their exit codes**

```
$ trent ...
exit=
```

**Output**
The relevant lines, pasted as text. Remove anything that names a secret's value.

**Environment**
- `trent --version` (or the commit, `git rev-parse --short HEAD`):
- How you run it (`npm run cli --`, `npm run cli:bun --`, or a binary):
- OS and version:
- Node version (`node --version`), and Bun if you use it:
- `trent doctor --json`: attach the file, or paste the checks that are not `ok`
