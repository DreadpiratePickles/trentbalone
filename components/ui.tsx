// Re-export all Trent UI primitives from the ui/ directory.
// This file exists so that `@/components/ui` resolves here (file takes
// precedence over directory in TypeScript's module resolution).
export * from "./ui/index";
