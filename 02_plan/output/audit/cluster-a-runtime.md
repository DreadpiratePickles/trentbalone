# Cluster A — Reliability & runtime (audit 2026-09-15)

| # | Item | Status | Evidence | Missing |
|---|---|---|---|---|
| 1 | Durable execution | EXISTS (web) | OrchestratorRun/Step/Event (@@unique runId,seq journal), JobRun; orchestrator-run-worker.ts:118 requeueRunningOrchestrationJobs on worker boot; hydrateOrchestrationRun; orchestrator-run-reconcile.ts:168; queue.ts:696 deterministic job ids (runId__step__stepId); orchestrator-durable-run.test.ts:160 resumes after restart w/o dup; WorkbenchAttempt/Checkpoint; durable-seat-runner.ts | CLI/core sessions persist transcripts (sessions/SessionStore.ts) but no journaled resumable tool loop in CLI |
| 2 | Idempotency / outbox / DLQ | PARTIAL | queue.ts:678 idempotent jobId, attempts 3, backoff; queue.ts:1079 platform-action retry ledger; resend Idempotency-Key; governance/IdempotencyManager.ts (executeWithIdempotency, getDeadLetterQueue); gateway/queue/MessageQueue.ts durable outbox, maxAttempts->dead, CircuitBreaker | IdempotencyManager in-memory and NOT wired anywhere; no outbox in web (JobRun + BullMQ add separate writes); worker.on("failed") only logs — no DLQ view; readiness-controls.ts:367 self-reports partial |
| 3 | Cron w/ history, enable/disable, delivery | PARTIAL | web: RecurringTaskTemplate enabled/cadence/nextRunAt, api/recurring-tasks PATCH, scheduler.ts:27, schedule-grammar.ts parseSchedule/nextFromCron; JobRun; CLI: tools/cron/index.ts cronjob_manage create/list/update/pause/resume/remove/run + deliver field + validateCronExpression + scanPromptForInjection | **CLI cron has no runner ("Nothing here ticks"); `trent cron` not a registered command (docs/doctor.md:189)**; web: no per-template run history, no per-template delivery, cadence daily|weekly only |
| 4 | Heartbeat mode | PARTIAL | heartbeat.ts:279 runCompanyHeartbeat (hourly via api/heartbeat/sweep CRON_SECRET) fixed rule checklist -> skip/monitor/act; autonomy-scheduler.ts:72 runAutonomousSweep in worker; scheduler.ts:317 assembleMorningBriefing -> morning-briefing-email.ts (email only) | checklist hard-coded not HEARTBEAT.md; no NO_REPLY LLM-turn; **supervision/quiet-hours.ts evaluateQuietHours exists but is unused**; no gateway DM delivery |
| 5 | Versioned agent definitions | PARTIAL | store/StorePort.ts:244 SkillDraftRow (skill|prompt, contentHash, live/archived/stale/rejected); improve/lifecycle.ts:90 promoteDraft, :220 rollback(iterationId), retireSkills; eval-gate.ts:71 promoteCandidate; suite versions content-hashed | Agent Prisma model no version column; no version object bundling prompt+model+tools; no labels; no in-flight pinning; CORE_ROLES static |
| 6 | Concurrency/fairness/priority | PARTIAL weak | orchestrator.ts:49 ORC_MAX_CONCURRENCY=4 per run; spend gating; AUTONOMY_SWEEP_COMPANY_IDS | no BullMQ priority, no per-company keys/caps, single Worker default concurrency |
| 7 | Double-texting policy | ABSENT | repl/engine.ts:201 `if busy return` (drop); interrupt.ts Ctrl+C abort; GatewayManager.ts:194 handleInbound no per-chat serialization | no reject/enqueue/interrupt/rollback policy, no per-conversation queue |
| 8 | Sandbox pause/resume/snapshot/fork | PARTIAL | workbench-provider.ts:130 restore?/snapshot?/checkpoint hooks; api/workbench/[id]/restore; app_solo_resume; WorkbenchCheckpoint; build-loop auto-checkpoints; E2B Sandbox.connect | full checkpoint only in workbench-local-provider.ts:942; E2B/Daytona snapshot = file-tree hash; no fork, no betaPause, no warm pool; CLI `trent sandbox` only `build` |
| 9 | Pluggable backends | PARTIAL | web: mock_local/e2b/daytona/railway via WORKBENCH_DEFAULT_PROVIDER; CLI: config TerminalBackendSchema docker|ssh|e2b|local | **CLI SSHBackend.ts:26 and E2BBackend return mock strings** (docs/terminal.md:25-28); real CLI = docker+local; web/CLI sets disjoint |

Surprises: stale-run detection + table-backed approval waiters + replay timeline exist; gateway
outbound queue already is a durable outbox with DLQ status; cron tool ports Hermes injection scan
and requires approval when terminal toolset enabled; improve ledger has iteration rollback; egress
sandbox (proxy+CA+broker) stronger than web providers.
