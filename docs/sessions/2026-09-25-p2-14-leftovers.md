# 2026-09-25 — P2-14: leftovers from dec2a54 and 3dd461c

Branch `feature/trent-fleet-v2`, HEAD f0d0ce4 at start. No subagents; no commit, stash, checkout or
push; no secret read or printed; no live call to any provider (every fake sits behind the REAL
egress proxy, dialled through `upstreamOverrides`). `TRENT_QUEUE_FALLBACK=disabled` in every shell;
vitest from the repo root. A background full-suite run on a separate worktree is not this session's.

Items (each red first):
1. Business toolset and MCP http servers through the egress proxy keep their own credential
   (`x-trent-own-credential`, the marker dec2a54 gave the broker for a2a).
2. Service-log test hygiene: every case in a temp dir; `ServiceLog` refuses an empty path.
3. Queue editing keeps a post's files; `trent cron queue list` shows each file's name and size.
4. Pack wording left stale by 3dd461c (Buffer "text only", "the owner posts" every clip, the
   pack-skills media_url message).
5. `seat-wiring.ts`: `a2a: "a2a.send"` in `TOOLSET_APPROVAL_GATES`.

## Status log (appended during the session)
- 20:20 Read: rulebook, `egress/CredentialBroker.ts`, `EgressProxy.ts` (forward path, `upstreamOverrides`),
  `tools/a2a/{build.ts,egress.test.ts}`, `tools/business/{http,index,build}.ts` + harness,
  `tools/mcp/{client,index,config}.ts` + tests, `connect/resolver.ts`, `service/service-log*.ts`,
  `tools/cron/queue-edit*.ts`, `apps/cli/.../cron-queue.ts` + test, `tools/social/{types,media-files,index,publish}.ts`,
  `fleet/{FleetPacks,pack-personas}.ts` + tests, `orchestrator/seat-wiring.ts` + test, docs.
- 20:20 Findings before code:
  1. Business: `createBusinessAdapter` builds ONE transport (the egress fetch) for both the
     provider calls and `createTokenResolver({ fetchImpl })`, so an OAuth refresh (Google,
     Square) takes the same broker path as the call. Without the marker the broker deletes the
     provider bearer and writes its own credential: `Authorization: Bearer <model key>` for
     Stripe, Square, Twilio (and the refresh on `connect.squareup.com`), and for any
     `*googleapis.com` host no Authorization at all plus `x-goog-api-key: <model key>`.
  2. MCP http: `connectHttp` sends the configured `headers` through the same egress fetch; the
     broker replaces `Authorization` with the model key on every MCP request.
  3. The stray `.1` (a,b,c,d) is the `tailServiceLog` fixture written with `${file}.1` while `file`
     was empty: `mkdirSync(".")` succeeds, `.1` lands in the cwd (the repo root under vitest),
     then `writeFileSync("")` throws. The committed test computes the path from a temp dir, but
     writes fixtures with bare `fs` calls, so nothing stops the same mistake again.
  4. Queue edit: `previewEdited` builds the social adapter with `workspace: profileDir`, so the
     post's files (relative to the seat's workspace) cannot simply be re-previewed there; the
     approved files carry their real path (`file`) and the path as named (`path`), from which the
     workspace they were resolved under is recoverable.
- 20:21 Baseline: `npx vitest run packages/trent-core/src/tools/business packages/trent-core/src/tools/mcp packages/trent-core/src/tools/a2a packages/trent-core/src/egress packages/trent-core/src/service packages/trent-core/src/tools/cron packages/trent-core/src/fleet/FleetPacks.test.ts packages/trent-core/src/fleet/pack-skills.test.ts apps/cli/src/commands/__tests__/cron-queue.test.ts` exit 0, 25 files, 207 tests.
- 20:25 Item 1 RED. `tools/business/egress.test.ts` (new; harness gains an `egress` option): real
  `EgressProxy`, broker token carrying a model-key fixture, each fake dialled through
  `upstreamOverrides` under the provider's real host name. `npx vitest run packages/trent-core/src/tools/business/egress.test.ts`
  exit 1, 4 failed: Stripe, Square and Twilio received `Bearer model-key-fixture-...`, Google
  received no Authorization. `tools/mcp/egress.test.ts` (new; a JSON-response Streamable HTTP
  fake): exit 1, 2 failed: `POST initialize: expected 'Bearer model-key-fixture-...' to be 'Bearer mcp-own-bearer-fixture'`,
  and a server with no header got the model key.
- 20:28 Item 1 fix. `tools/business/http.ts` `withOwnCredential` sets `x-trent-own-credential` on
  every request; `index.ts` wraps the egress fetch with it, and the same transport goes to
  `createTokenResolver`, so the OAuth refresh carries it too. `tools/mcp/client.ts` adds the
  marker to `requestInit.headers` when the transport is the egress fetch (not the test seam).
  Business + MCP + a2a + egress suites: exit 0, 17 files, 113 tests.
- 20:31 Item 1 refresh proof: a fifth case captures the transport `createBusinessAdapter` hands
  `createTokenResolver` (delegating `vi.mock`) and POSTs a refresh form on it to
  `oauth2.googleapis.com` through the proxy. With the wrapper removed from `index.ts` (restored
  from a scratch copy right after): exit 1, 5 failed, the token endpoint got `x-goog-api-key: model-key-fixture-...`.
  Restored: business + mcp exit 0, 9 files, 54 tests. Docs: one paragraph each in
  `docs/business.md` (step 3) and `docs/mcp.md` ("http goes through egress").
- 20:34 Item 2 RED. `service/service-log.test.ts`: fixture writes go through `inTemp` (refuses a
  path that is not absolute and inside the case's temp dir; `${""}.1` is refused), each case
  checks the cwd holds no `.1`, `service.log` or `service.log.1`, and a new case expects
  `new ServiceLog({ file: "" | "   " })` to throw. `npx vitest run packages/trent-core/src/service/service-log.test.ts`
  exit 1, 1 failed (`"": expected [Function] to throw an error`), 7 passed.
- 20:35 Item 2 fix: the `ServiceLog` constructor throws on an empty or blank path
  (`service/service-log.ts`, one guard; not on the owned list, but the guard test needs it).
  `npx vitest run packages/trent-core/src/service` exit 0, 5 files, 54 tests;
  `apps/cli/src/commands/__tests__/service.test.ts` (the daemon's own ServiceLog) exit 0.
- 20:40 Item 3 RED. `tools/cron/queue-edit.test.ts` (5 new cases on a Bluesky post with two real
  PNG fixtures resolved in a separate workspace): `npx vitest run packages/trent-core/src/tools/cron/queue-edit.test.ts`
  exit 1, 3 failed: the edited card lacked the files (`expected 'post to bluesky ...' to contain 'media-out/brunch.png (2,048 bytes, ...'`),
  an edit after a file changed resolved instead of rejecting, the list row had no `media`. (The
  unchanged-edit and media-URL-replaces cases pass before and after: they pin the rule.)
  `apps/cli/.../cron-queue.test.ts` new list case: exit 1, 1 failed (no `media` in `--json`).
- 20:44 Item 3 fix. `queue-edit.ts`: the files stay unless the patch names `mediaUrl`; the new
  call's args carry `media: [{path, alt}]`; the workspace the files were approved in is recovered
  from `file` minus the normalized `path` (all files must agree; absolute-only paths take their
  deepest common directory, never `/`); the dry run re-resolves them there and the edit is refused
  unless each approved file's `describeMediaFile` line is on the new card; `sameRequest` compares
  files; `QueuedPostRow.media` is `[{path, bytes}]`. `cron-queue.ts` list prints
  `files: <path> (<n> bytes), ...` under the text. `npx vitest run packages/trent-core/src/tools/cron apps/cli/src/commands/__tests__/cron-queue.test.ts packages/trent-core/src/tools/social`
  exit 0, 8 files, 72 tests. Doc: one paragraph in `docs/social.md` after "What the human approves".
  Not changed (not owned): `docs/cron.md` line 120 still describes `queue edit` without the files
  rule; it says nothing false.
- 20:47 Item 3 addendum: a sixth case (a deleted file refuses the edit with `social_media_missing`,
  approval left alone) was written after the fix to back the docs sentence; queue-edit exit 0, 15 tests.
- 20:50 Item 4 RED. `fleet/pack-skills.test.ts`: the media_url message is one constant
  (`MEDIA_URL_PROBLEM`) and the parser fixture pins what it must say
  (`/Bluesky refuses it .*Buffer sends it only from a URL the owner hosts/`); a new case checks no
  market pack's state or persona says "text only" and the creator state and persona name Bluesky
  and "its own approved post". `npx vitest run packages/trent-core/src/fleet/pack-skills.test.ts`
  exit 1, 2 failed (the message; `social: expected 'Posts, scheduled posts, ...' not to match /text only/i`).
- 20:52 Item 4 fix. `FleetPacks.ts` social state: Bluesky takes up to four images or one clip from
  the workspace with alt text; Buffer takes an image or a video only from a URL the owner hosts.
  Creator state: nothing is uploaded or published by this crew; a cut clip goes to Bluesky only as
  its own approved post where the social toolset is on and Bluesky is connected, the owner posts
  it anywhere else. `pack-personas.ts` creator closing line says the same; persona lengths 1782,
  1724, 1725 (cap 1800). `npx vitest run packages/trent-core/src/fleet` exit 0, 51 files, 425 tests.
  Not changed (not owned), same stale "a Facebook Page; text only": `README.md:117`,
  `docs/fleet.md:242`, and the generated `improve/fixtures/docs-corpus/corpus.json`.
- 20:55 Item 5 RED. `tools/a2a/registration.test.ts` "registered everywhere" gains
  `expect(TOOLSET_APPROVAL_GATES.a2a).toBe("a2a.send")` (the pattern `business/registration.test.ts:47`
  uses for `business`; this test file is not on the owned list, it is where the pattern lives).
  `npx vitest run packages/trent-core/src/tools/a2a/registration.test.ts` exit 1, 1 failed
  (`expected undefined to be 'a2a.send'`).
- 20:56 Item 5 fix: `orchestrator/seat-wiring.ts` `a2a: "a2a.send"` beside `business`.
  `npx vitest run packages/trent-core/src/tools/a2a packages/trent-core/src/orchestrator/seat-wiring.test.ts` exit 0, 4 files, 20 tests.
- 21:00 Verify (all from the repo root, `TRENT_QUEUE_FALLBACK=disabled`):
  ```
  npx vitest run packages/trent-core/src/tools packages/trent-core/src/egress packages/trent-core/src/service \
    packages/trent-core/src/fleet packages/trent-core/src/orchestrator/seat-wiring.test.ts \
    packages/trent-core/src/orchestrator/seat-wiring.per-seat.test.ts apps/cli/src/commands/__tests__/cron-queue.test.ts \
    apps/cli/src/commands/__tests__/docs-truth.test.ts apps/cli/src/commands/__tests__/service.test.ts \
    packages/trent-core/src/wrapped-modules.test.ts          # exit 0, 114 files, 919 passed, 19 skipped
  (cd packages/trent-core && npm run build)                  # exit 0
  npx tsc --noEmit -p apps/cli/tsconfig.json                 # exit 0
  node scripts/ci/repo-scan.mjs                              # exit 0
  git status --short                                         # only the files below, plus the pre-existing
                                                             # parity-push.md edit and notes/; no .1 or service.log in the root
  ```
  Largest touched file: `tools/cron/queue-edit.ts` 317 lines; every touched file under 500.

## Files changed
Owned: `tools/business/{http.ts,index.ts}`, `tools/business/egress.test.ts` (new),
`tools/business/testing/harness.ts` (test support: an `egress` option), `tools/mcp/client.ts`,
`tools/mcp/egress.test.ts` (new), `service/service-log.test.ts`, `tools/cron/queue-edit{,.test}.ts`,
`apps/cli/src/commands/groups/cron-queue.ts` (list render and its description),
`apps/cli/src/commands/__tests__/cron-queue.test.ts`, `fleet/{FleetPacks.ts,pack-personas.ts,pack-skills.test.ts}`,
`orchestrator/seat-wiring.ts`, `docs/{business,mcp,social}.md`, this log.
Outside the list, each needed by its item: `service/service-log.ts` (the empty-path guard the item-2
test asks for), `tools/a2a/registration.test.ts` (the item-5 red test, where the gate-map pattern lives).
`egress/CredentialBroker.ts` unchanged: no broker defect, the callers now opt in.

## Follow-ups (not done here)
1. `README.md:117` and `docs/fleet.md:242` still say Buffer is "a Facebook Page; text only" (the
   pack state no longer does); `improve/fixtures/docs-corpus/corpus.json` carries the same copy.
2. `docs/cron.md:120` describes `queue edit` without the files rule (not false, just silent).
3. `tailServiceLog("")` still reads `.1` from the cwd; only the writer refuses an empty path.
