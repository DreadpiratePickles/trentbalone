# 2026-09-26 — L0-4: doctor "Local Model" check, connectivity (G13), workbench (G12), credentials line

Branch `feature/trent-fleet-v2`, HEAD `412f5b0` at start. Other agents edit the tree concurrently; this task
owns `packages/trent-core/src/doctor/**`, `docs/doctor.md`, the check count in `README.md`, and this log.
No commit, stash, checkout or push. No cloud model call. Ollama 0.32.9 on 127.0.0.1:11434.

## Inputs read
- `AGENTS.md`, `docs/development_methodology_and_coding_rulebook.md`.
- `01_discovery/output/trent-local-path-audit-2026-09-26.md` (G8, G12, G13, and G17 for the context read).
- `01_discovery/output/local-models-2026-09-26.md` sections 0.1, 1, 5, 8.5 (D1-D3).
- `02_plan/output/local-models-plan-2026-09-26.md` (L0 row "G8, G13" and "G12").

## Facts established before changing anything
- `doctor/checks/credentials.ts:53-59`: a keyless provider is a `skip` with "runs locally and needs no API key";
  nothing probes the runtime, so `trent doctor` exits 0 with Ollama down (audit G8).
- `doctor/checks/connectivity.ts:12`: `dns.lookup("api.openai.com")` whatever the provider (G13).
- `doctor/checks/workbench.ts`: Docker daemon down, binary missing or unresponsive is a `fail` (exit 3).
  The runtime does not fail there: `apps/cli/src/repl/tools.ts:178-186` `resolveSandbox` returns the local
  backend whenever `docker info` does not answer, and `runtime/headless.ts:277-285` (`trent run`) uses the
  same `wireTools`. `config/defaults.ts:29` defaults `terminal.backend` to `docker`, and the schema
  (`config/sections/terminal.ts`) has no key that turns the fallback off. So the doctor disagrees with the
  REPL and `trent run` today (G12).
- `DoctorRunner.ts`: every check gets 15 s and the run 60 s. A local model's cold prefill of a 4K-token prompt
  can alone exceed 60 s on this machine (audit: 34.7 tok/s prompt eval on the 27B), so the new check needs a
  budget of its own that does not starve the checks after it.
- Ollama `/api/ps` reports `context_length` for a loaded model (measured: `qwen3.5:9b` loaded at 32768);
  `/api/show` `parameters` carries `num_ctx` only when a Modelfile sets it (absent for `qwen3.5:9b`);
  `model_info.<arch>.context_length` is the training maximum (262144), not the effective window.
- LM Studio `GET /api/v1/models` lists `loaded_instances[].config.context_length` and `.parallel`
  (https://lmstudio.ai/docs/developer/rest/list, read 2026-09-26). llama.cpp `GET /props` carries
  `default_generation_settings.n_ctx` and `total_slots` (server README, read 2026-09-26).
- The wrapper's tool-call contract is JSON in content (`{"toolCall":{"name","action":"<tool> <json>"}}`,
  `apps/web/lib/model-gateway.ts:341-343`, parsed by `tools/action.ts` `parseAction`), not native function
  calling, so the smoke test scores that contract.

## Requirement change recorded before editing a test (rulebook 11: a changed requirement is documented first)
- G12: "Docker daemon down" moves from `fail` to `warn` because the REPL and `trent run` fall back to the
  local backend in that state. The three workbench tests in `doctor/checks/inspection.test.ts` that asserted
  `fail` for daemon-down, binary-missing and the measured real probe are updated to the new requirement:
  `warn`, naming the local fallback and its lack of isolation. Nothing else in that file changes.

## Plan
1. Red: `DoctorRunner.test.ts` expects category "Local Model" and a check-declared budget; new
   `checks/local-model.test.ts` against a fake runtime (fetch seam); new `checks/connectivity.test.ts`
   (DNS seam); workbench and credentials tests as above.
2. Green: `checks/local-model.ts` (+ `local-runtime.ts` probes, `local-smoke.ts` smoke and TTFT),
   `DoctorCheck.timeoutMs`, runner budget, connectivity, workbench, credentials message.
3. Docs: `docs/doctor.md` row 23 and count; README count.
4. Live doctor on a scratch `TRENT_HOME` (provider ollama), then with the base URL on a closed port.

## Log
- Red (exit 1): `npx vitest run` on DoctorRunner, local-model, connectivity, inspection, credentials tests.
  Failures, each for the intended reason: DoctorRunner "Local Model" missing from the categories and
  `timeoutMs` ignored (the declared-budget check timed out at the 100 ms default; the wedged one ran to
  10 s); local-model.test.ts "Cannot find module './local-model.js'"; connectivity: message is the old
  "Network connectivity verified", no DNS lookup for generativelanguage.googleapis.com, api.anthropic.com,
  api.groq.com; workbench daemon-down / binary-missing / timeout "expected 'fail' to be 'warn'";
  credentials "runs locally and needs no API key" does not contain "runs locally, no key needed".
- Green: `checks/local-model.ts` (the check), `checks/local-runtime.ts` (runtime probes: Ollama, llama.cpp,
  LM Studio, generic), `checks/local-smoke.ts` (five cases, scored on the seat contract through
  `createModelGateway` with a fetch-seam `streamProvider`), `checks/local-stream.ts` (SSE transport and
  time to first token; request body and usage from the gateway's `buildCompatChatBody`/`parseCompatUsage`),
  `doctor/endpoint.ts` (provider -> real base URL/host), `DoctorCheck.timeoutMs` + runner budget,
  `DoctorContext.lookupHost`, connectivity, workbench, credentials. The same five test files: 64 passed.
- docs-truth went red once the registry held 23 (exit 1): `README.md: "22 checks"` twice (lines 47 and 265)
  and `docs/doctor.md: "22 checks"` twice. Fixed: those four numbers to 23 (README: only those two
  numbers), doctor.md row 23 plus a "The Local Model check" section, and rows 2, 7 and 13 rewritten for the
  changed behaviour. Then `npx vitest run packages/trent-core/src/doctor apps/cli/.../behaviour.test.ts
  apps/cli/.../docs-truth.test.ts packages/trent-core/src/wrapped-modules.test.ts`: exit 0, 24 files, 203 tests.
  (There is no `diagnostics.test.ts`; the doctor CLI tests live in `behaviour.test.ts`.)
- `node scripts/ci/repo-scan.mjs`: exit 0 (1116 files, three PASS).
- Left stale, not mine to edit: README.md:49's proof comment still records "failed 2 ... Sandbox & Workbench:
  Docker daemon stopped" from a run before G12; docs/doctor.md's "reports `total 20`" sentence predates this.
- Live model choice: `ollama list` shows `qwen3.5:9b` at 6.6 GB (pulled by L0-3) and `qwen3-embedding:0.6b`
  (an embedder, not a chat model). No chat model is under 6 GB. The 9B is the plan's small model
  (02_plan "Qwen3.5-9B Q4, ~5.7 GB") and lighter than the 27B fallback, so the full check ran on it; the
  size discrepancy is reported. Another agent had it loaded at the same time, so timings may include queueing.
- Live 1 (exit 1, 25 s, not a result): the CLI died loading `config/schema.ts` ("RecoveryConfigSchema is not
  defined") while another agent restructured `config/`. `doctor --dry-run --json` then listed 23 checks
  (exit 0), so the run was repeated.
- Live 2: scratch TRENT_HOME, `config set provider ollama` (0), `config set model qwen3.5:9b` (0),
  `env -i ... tsx apps/cli/src/index.ts doctor --json`: **exit 0**, real 374.6 s, total 23, passed 13,
  warnings 5, failed 0, skipped 5. The Local Model line (warn):
  "Ollama 0.32.9 at http://127.0.0.1:11434/v1 serves qwen3.5:9b: context 32768 tokens (the loaded model,
  /api/ps); parallel slots not exposed (OLLAMA_NUM_PARALLEL, default 1); tool-call smoke 1/5 (failed:
  call: path was undefined; escaping: no reply within 60 s; required: toolCall.action was not a "<tool>
  <json>" string; unknown-tool: no reply within 60 s); no first token within 60 s at a 4096-token prompt."
  Credentials: "Provider "ollama" runs locally, no key needed; ...". Connectivity: "Local runtime at
  http://127.0.0.1:11434/v1 answered (HTTP 200); ... no cloud host was resolved."
- The timings are not representative of the model: the Ollama log shows two `/api/generate` calls from other
  agents (1m32s, 1m27s) overlapping the smoke and first-token requests, `uptime` load average ~1000, swap
  14.8 of 15.4 GB used. With Ollama otherwise idle, one 170-token smoke prompt still took 36.7 s.
- The failures are the model's, not the parser's: a direct probe (`tsx probe-cases.mts call required`, exit 0,
  scratchpad) returned `{ "toolCall": { "name": "file_ops", "action": "read_file", {"path": ...} }` and an
  `action` given as an object; both are not the seat's JSON, and `readTurn` says so.
- Live 3: same profile with `OLLAMA_BASE_URL=http://127.0.0.1:9/v1` (curl to it: exit 7): **exit 3** in 1.5 s;
  Local Model fail "No local model runtime answered at http://127.0.0.1:9/v1 (no connection), and provider
  "ollama" sends every model call there." fix "Start Ollama (`ollama serve`, or open the Ollama app), or point
  OLLAMA_BASE_URL at the server that runs it."; connectivity warn naming the same URL; `api.openai.com`
  appears nowhere in the report (grep count 0).
- G12 could not be shown live: Docker is up on this machine, and the scratch profile was written with
  `terminal.backend: local`. It is covered by the three fake-exec tests in inspection.test.ts.
- First-token filler: the first version was digit-heavy (Qwen splits digits), so it was rewritten as prose
  and measured once on the live model: 16437 characters = 3718 prompt tokens (`tsx count-prompt.mts`,
  exit 0, 100.3 s under the load above), 4.4 characters a token; the filler now targets 4096 at that rate.
- `cd packages/trent-core && npm run build` (tsc --noEmit): exit 2, one error, not in doctor/**:
  `src/model-gateway/local-routing.test.ts(153,48)` (another agent's file). `npx tsc --noEmit -p
  apps/cli/tsconfig.json`: exit 2, one error, not in doctor/**: `packages/trent-core/src/model-gateway/
  local-runtime.ts(158,64)` (another agent's new file; unrelated to doctor/checks/local-runtime.ts).
- Final round after the last edits: `npx vitest run packages/trent-core/src/doctor apps/cli/src/commands/__tests__/
  behaviour.test.ts apps/cli/src/commands/__tests__/docs-truth.test.ts packages/trent-core/src/wrapped-modules.test.ts`
  exit 0 (24 files, 208 tests; the 5 over 203 are L0-5's embedder tests); `node scripts/ci/repo-scan.mjs` exit 0;
  `cd packages/trent-core && npm run build` exit 0; `npx tsc --noEmit -p apps/cli/tsconfig.json` exit 2, the one
  error in another agent's `model-gateway/local-runtime.ts(158,64)`, none in doctor/**.
- Noted for the owner of `doctor/checks/embedder.ts` (L0-5 edits it concurrently): it waits up to 40 s for a
  local embed, but the runner gives a check 15 s unless the check declares `timeoutMs` (added here).

## State at close
Implemented and unit-tested; live-verified on this machine against Ollama 0.32.9 (reachability, version,
model, context via /api/ps, smoke through the gateway, first-token deadline, closed-port exit 3). Not
committed (the brief forbids it). Timings from the live run are contaminated by load and are not a
measurement of qwen3.5:9b.
