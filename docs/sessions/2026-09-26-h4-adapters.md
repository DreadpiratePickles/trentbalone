# H4 messaging adapters: Matrix, Mattermost, LINE, ntfy, 2026-09-26

Gap 5 of `01_discovery/output/harness-landscape-2026-09-26.md` (line 24): 8 chat platforms against
OpenClaw 20+ and Hermes ~30; wave H4 adds the four with clean public APIs. Owner: H4 (one Opus
agent, no subagents). Ownership: new `packages/trent-core/src/gateway/platforms/{matrix,mattermost,
line,ntfy}.ts` with a `*.wire.test.ts` each, `// [H4]` hunks in `gateway/registry.ts` (and its
test), `gateway/index.ts`, the platform rows of `docs/gateway.md`, the secret-name lines of
`docs/configuration.md`, README's adapter count only, this log. Not touched: `GatewayManager.ts`
(S2), `WebhookServer.ts` and `config/sections/gateway.ts` (H3), model-gateway (L0/L1), solo (S1.1).

## Read first
- AGENTS.md, the rulebook (sections 7, 9, 11: verify webhook signatures before parsing; replay and
  idempotency tests; pin the API version; typed errors that never carry a token).
- The adapter shape: `platforms/telegram.ts` (long poll, offset cursor in `GatewayStore.cursors`),
  `platforms/signal.ts` (stream consumer, buttons as text), `platforms/whatsapp.ts` (signed
  webhook through `handleWebhook`, which `WebhookServer` serves at `/webhooks/<platform>`),
  `platforms/slack.ts` (global `WebSocket`, reactions). Attachments: 4a0e171 (lazy `open()`, only
  after pairing, credentials only to the platform's own host).
- Pairing is not the adapter's job: `GatewayManager.handleInbound` gates every `InboundMessage`
  (`security/PairingManager.ts`), so each wire test drives the real manager for the pairing case.
- Approval cards: `GatewayManager.sendApproval` renders buttons when `capabilities().buttons`, else
  the `APPROVE <id> <nonce>` text card; `ApprovalBridge.resolveReaction` already knows the unicode
  thumbs and the Slack-style names Mattermost uses, so no bridge change is needed.
- Lists that enumerate platforms: `registry.ts` + `registry.test.ts` ("exactly the eight"),
  `gateway/index.ts` exports, `docs/gateway.md` (intro, table, voice-note table, reactions),
  `docs/configuration.md` (secret names, voice notes), README "8 adapters". Setup and doctor do not
  enumerate platforms (`grep -rni whatsapp packages/trent-core/src/{setup,doctor}` is empty);
  `docs-truth.test.ts` checks no gateway count.

## Sources (the platforms' public API docs)
- Matrix: spec.matrix.org client-server API v1.11 (sync, send, join, joined_members, whoami,
  authenticated media, threading, annotations); the spec yaml in matrix-org/matrix-spec.
- Mattermost: api.mattermost.com (CreatePost, SaveReaction, GetUser, GetChannel, GetFile,
  WebSocket authentication_challenge and events); the yaml in mattermost/mattermost api/v4/source.
- LINE: developers.line.biz Messaging API reference (reply, push, bot info, get content, quick
  reply, postback, signature validation, webhook redelivery); the line/line-openapi yaml.
- ntfy: docs.ntfy.sh (publish as JSON, action buttons, http action, access tokens, JSON stream,
  since=, health check).

## Plan
1. RED: registry test says twelve; one wire test file per adapter against a fake server, each with
   the brief's cases (pairing code for an unknown sender, paired sender routed, outbound send,
   reply in thread where threads exist, buttons or reactions, LINE bad signature 401 with nothing
   routed, cursor persisted so a restart never replays).
2. GREEN: the four adapters, the registry and index entries.
3. Docs; verify with the brief's commands, exit codes recorded here.

## Progress

### RED (23:58 local)
`TRENT_QUEUE_FALLBACK=disabled npx vitest run packages/trent-core/src/gateway/registry.test.ts
packages/trent-core/src/gateway/platforms/{matrix,mattermost,line,ntfy}.wire.test.ts` -> exit 1:
5 files failed, 2 tests failed | 17 passed. The four wire files fail on `Cannot find module
'./matrix.js'` (and `./mattermost.js`, `./line.js`, `./ntfy.js`); registry.test.ts fails
"registers exactly the twelve spec platforms" (`expected [ 'discord', 'email', …(6) ]`) and the
capability matrix row (`expected undefined` for `matrix.matrix`). Tests written: per adapter, the
wire shape of send (auth header, body), reply in a thread (Matrix m.thread relation, Mattermost
root_id; LINE reply token then push), buttons as the platform has them (LINE quick-reply postback,
ntfy http actions, Matrix/Mattermost as text), reactions (Matrix m.annotation, Mattermost
reaction_added), the cursor (Matrix `matrix.since` next_batch, Mattermost `mattermost.last`, LINE
`line.seen` webhookEventIds, ntfy `ntfy.since` message id) with a second adapter over the same
store proving no replay, LINE's bad signature (401, nothing routed, nothing sent), lazy audio, and
through the real GatewayManager: pairing code for an unknown sender, routed once paired, and an
approval card decided by a reaction (Matrix, Mattermost), a postback (LINE, over real HTTP through
`WebhookServer` at `/webhooks/line`) or an action tap on the reply topic (ntfy).

### GREEN (00:10 local)
- `platforms/matrix.ts`, `mattermost.ts`, `line.ts`, `ntfy.ts`; `// [H4]` hunks in `registry.ts`
  (four entries, env names only), `index.ts` (four exports), `transport/http.ts`
  (`unsentAttachmentsAsText`: an adapter without `files` names what it could not send rather than
  dropping it silently).
- Same command as RED -> exit 0, 5 files, 55 tests. They passed on the first run, so each safeguard
  was broken on purpose and the file restored: no since persisted (matrix), no signature check
  (line, 2 tests fail), no webhookEventId dedupe (line), no watermark (mattermost), `since=all`
  (ntfy), own tag not skipped (ntfy) -> each exit 1 with the named test failing.
- `cd packages/trent-core && npm run build` -> exit 2: first run had 6 errors of mine (matrix.ts
  inference in the sync loop; one test helper type), fixed; the 6 left are all in
  `src/tools/browser/` (H5's in-flight attach work), none in a gateway file.
- Decisions: Mattermost buttons off (interactive-message presses reach an integration URL
  unsigned); ntfy's reply topic is the paired sender (ntfy has no sender identity) and its action
  buttons carry no Authorization header; LINE replies with the event's reply token while fresh
  (50 s, single use) and falls back to push on a 400; LINE and Matrix/ntfy cursors are written
  before dispatch (at most once, as Telegram's offset); Matrix encrypted rooms are not read.
- Finding, not mine to fix: `gateway start` opens the `WebhookServer` only when `gateway.webhooks`
  has an H3 route (`webhooks/serve.ts` `openWebhookRoutes`), so LINE's and WhatsApp's webhooks have
  no listener otherwise. Documented in gateway.md.

### Docs
- `docs/gateway.md`: "Twelve adapters", four table rows, the registry sentence, a "Matrix,
  Mattermost, LINE and ntfy" section (inbound, cursor, card, thread per platform; LINE signature
  and the listener caveat; ntfy's topic-as-credential model; why Mattermost has no buttons; Matrix
  E2EE), reactions (Matrix, Mattermost), voice-note table rows and "nine" wire files.
- `docs/configuration.md`: the secret-name list and the voice-note platforms.
- README: `8 adapters` -> `12 adapters`, nothing else.

## Verification (each command, its exit code)
- `TRENT_QUEUE_FALLBACK=disabled npx vitest run packages/trent-core/src/gateway packages/trent-core/src/setup packages/trent-core/src/doctor apps/cli/src/commands/__tests__/docs-truth.test.ts packages/trent-core/src/wrapped-modules.test.ts`
  - run 1 -> exit 1: 54 files passed, 1 failed; 447 tests passed, 1 failed, 1 skipped. The one
    failure is docs-truth "states the command counts" (README 35/151, registry 36/152 after S2
    registered `trent solo`), not an H4 line; H3's log records the same.
  - run 2 (after docs edits) -> exit 1: 53 passed, 2 failed; 446 passed. Both foreign:
    `wrapped-modules.test.ts` 500-line ceiling on `tools/index.ts` (506 lines, modified by
    another agent), and `discord.wire.test.ts` heartbeat `hb.d` null (a timing race under load:
    unmodified file, then 3 isolated runs each exit 0, 4/4). docs-truth passed in run 2.
- `TRENT_QUEUE_FALLBACK=disabled npx vitest run packages/trent-core/src/gateway apps/cli/src/commands/__tests__/docs-truth.test.ts`
  -> exit 0, 28 files, 231 tests passed, 1 skipped.
- `cd packages/trent-core && npm run build` -> exit 2; no error in any H4 file. Run 2: 6 errors,
  all `src/tools/browser/` (H5 in flight); run 3: 1 error, `src/orchestrator/seat-constrained.test.ts`.
- `cd apps/cli && npx tsc -p tsconfig.json --noEmit` -> exit 0 (the four new exports compile there).
- `node scripts/ci/repo-scan.mjs` -> exit 0 (canned, hex, emoji all PASS).
- Real CLI: `TRENT_HOME=<scratch dir> NTFY_TOPIC=<dummy topic> npm run -s cli -- gateway status --json`
  -> exit 0, `count 12, configured 1`, ntfy set, the other eleven not set.
- `apps/cli/src/commands/__tests__/gateway-webhooks.test.ts` "[H3] ... serves the routes" fails
  (500 vs 202) with and without the four H4 registry entries (registry.ts backed up, entries cut,
  re-run, restored): not H4.

## Open, for other owners
- S2/H3: `gateway start` opens the `WebhookServer` only when `gateway.webhooks` has a route, so
  the chat adapters' webhooks (`/webhooks/line`, `/webhooks/whatsapp`, Telegram webhook mode, the
  Slack Events API) have no listener otherwise. Opening it when a webhook-only adapter is
  configured would make LINE and WhatsApp inbound work from `gateway start` alone.
- S2: `gateway setup <platform> --token` writes `<PLATFORM>_BOT_TOKEN`; among the new four that
  names only Mattermost's secret; Matrix, LINE and ntfy each need two settings or other names.
- Comments that still list the older platforms, left to their owners: README's proof comment
  (the Messaging row names eight directories), `voice-notes.ts` header, `config/sections/gateway.ts`
  voice_notes comment (H3), `GatewayManager.ts:274` (S2).
- No `*.live.test.ts` for the four (not in the brief; no real accounts).
