# C11: solo runs constrained, and is proven on a real model, 2026-09-26

Task (from the orchestrating session): council item C11 (`02_plan/output/hermes-council-verdict-2026-09-26.md`,
Tier 3) and section 7 item 4 (the default mode is decided by the 9B solo-format smoke score; this lane
reports the score and decides nothing). No subagents, no commits, no stash/checkout/reset/push. Every hunk
in an existing file carries `// [C11]`. `solo/prompt.ts` is not edited (C15 rewrites the prompt; this lane
only calls `soloResponseFormat`); `tools/memory/**`, `governance/**`, `service/**` are not touched.

## Log
(Times are the machine clock, EDT; a `~` marks an entry whose minute was estimated afterwards, between two
clock readings: 03:10:11 from vitest's own "Start at", 03:22 from `uptime`.)
- 03:09 EDT start. `uptime`: load averages 92.34 85.90 125.07 (the machine's baseline is high from
  unrelated processes). HEAD 0ee887c on feature/trent-fleet-v2 (the brief said 5f33024: another lane
  committed C2 meanwhile). Read AGENTS.md, the rulebook (Universal Coding Rules, Phase 4, Phase 6), C11 and
  section 7 of the council verdict, docs/solo.md, docs/local-models.md, the L1 log, solo/{prompt,runner,turn,
  parse,types}.ts, runner-for-mode.ts (397 lines), solo-continuity.ts, config/sections/agent.ts, the doctor's
  local-model.ts, local-smoke.ts, local-stream.ts and their tests.

## Findings that shape the design (read from source)
- `soloResponseFormat` (prompt.ts:137) has no production caller; `createSoloRunner` passes
  `config.responseFormat` and `config.maxToolCalls` through (runner.ts:121,127) and `parseReply` reads the
  envelope when a format was sent (turn.ts:304). `runner-for-mode.ts` builds `config` with neither.
- `agent.solo.max_tool_calls` is read by nothing, and `config/sections/agent.ts` has no `solo` key: the
  `agent` block is a passthrough, so any value parses. `ConfigManager` validates on load and save.
- The rule for "constrained output applies here" already exists for seats: `constrainedOutputApplies()`
  (model-gateway/local-runtime.ts, L1): a local alias, unless `models.local.constrained_output` is false;
  `all` adds hosted providers. Solo reuses it, so one setting governs both modes; by default a hosted
  provider keeps the `<tool_call>` text protocol.
- The alias env (`TRENT_MODEL_ALIAS`) is written by the orchestrator's constructor (`applyModelEnv`), which
  the headless runtime builds before the runners (headless.ts:370 then :429), the same env `soloWindowTokens`
  already reads.
- Compaction's model calls never carry the format (compaction.ts:149 omits it): the summary stays prose.
- A delegated solo child runs on the same gateway, so it needs the envelope too; its tools are
  `childAdapters(soloTools)` (no human tools), so its enum is built from those.
- The doctor's smoke (`local-smoke.ts`) sends the seat format through its own transport
  (`local-stream.ts`), which drops `input.responseFormat`: a solo variant needs that one field forwarded.
- 03:10 item 1a RED: `npx vitest run packages/trent-core/src/config/agent-solo-schema.test.ts` -> exit 1,
  2 failed / 2 passed: `max_tool_calls: 0: expected true to be false` (the passthrough accepted 0) and the
  ceiling constant absent. GREEN after the `[C11]` block in config/sections/agent.ts
  (`AgentSoloConfigSchema`, `SOLO_MAX_TOOL_CALLS_CEILING = 500`): the same file with agent-schema.test.ts and
  schema-split.test.ts -> exit 0, 3 files / 12 tests (the split snapshot is unchanged: its inputs hold no
  `agent.solo`).
- ~03:12 item 1b RED: `npx vitest run apps/cli/src/runtime/runner-for-mode.constrained.test.ts` (new) -> exit 1,
  3 failed / 2 passed. Local alias: `expected undefined to match object { type: 'json_schema', ... }` (no
  response_format; the envelope reply was then taken as the answer); delegated child: 1 request instead of
  3 (same cause); `agent.solo.max_tool_calls: 2`: 4 requests instead of 3 (the cap was the default 25). The
  two passing cases are guards that must stay true: a hosted provider sends no format; `models.local.
  constrained_output: false` sends none.
- ~03:13 GREEN: new `packages/trent-core/src/solo/turn-settings.ts` (`soloTurnSettings(config, adapters, env)`:
  `responseFormat` when `constrainedOutputApplies(env)`, `maxToolCalls` from `agent.solo.max_tool_calls`),
  called from runner-for-mode.ts for each conversation's runner (over the adapters it gets) and for the
  delegated child (over `childAdapters(soloTools)`), four `[C11]` hunks, 397 -> 401 lines. Same file -> exit
  0, 5/5. Neighbours: runner-for-mode.test.ts -> exit 0, 14/14; solo-continuity.test.ts -> exit 0, 5/5.
- ~03:15 item 2 RED: `npx vitest run packages/trent-core/src/doctor/checks/local-smoke-solo.test.ts` (new; a stub
  `local-smoke-solo.ts` with exports only, so the red is behavioural) -> exit 1, 6/6 failed: the check's
  details have no `soloSmoke`; `readSoloTurn` returns invalid for an envelope call, an answer and a toolset
  name. The fake runtime (`local-runtime.test-helpers.ts`, `[C11]` hunks) now marks a request carrying the
  solo envelope (`solo: true`), finds its case at the end of a solo turn's opening, and answers it with a
  well-behaved envelope (`wellBehavedSoloReply`).
- ~03:17 item 2 GREEN. `local-smoke.ts` `[C11]` hunks: `Turn` exported; `runSmoke(input, format)` with a
  `SmokeFormat` (messages, read, optional responseFormat), the seat format the default (`SEAT_SMOKE_FORMAT`).
  `local-stream.ts` `[C11]`: the doctor's transport forwards `responseFormat` into the body (it dropped it).
  New `local-smoke-solo.ts`: per case one `file_ops` adapter holding the case's tool (its instructions are the
  seat case's own `renderToolInstructions`), the solo system prompt (`buildSystemPrompt`, default persona), the
  case as the turn opening after the context tier, `soloResponseFormat` as constrained output, `parseReply`
  (envelope on) mapped to the judge's turn. `local-model.ts` `[C11]`: both formats from one input,
  `solo-format smoke N/5` in the message, `details.soloSmoke`, fail at 0/5 and warn below 5/5 only when
  `agent.mode` is solo, the deadline counts ten cases. `local-smoke-solo.test.ts` -> exit 0, 6/6, after one
  correction to the TEST: I had expected "called read_file instead of write_file" for the escaping case, but
  read_file is not among that case's tools, so the parser rightly says "called read_file, which was not
  offered". `local-model.test.ts` -> exit 1 then (6 chat calls expected, 11 made: the new requirement), its
  count updated with a `[C11]` hunk -> exit 0, 17/17.
- ~03:19 new `packages/trent-core/src/solo/solo.live.test.ts` (TRENT_TEST_LIVE=1 only: the vitest config excludes
  `*.live.test.ts` otherwise, and the file skips itself, printing "A skip is NOT a pass"). Three cases, run one
  at a time with `-t`: `qwen` (three turns on one conversation through Ollama under the `ollama` alias as
  `applyModelEnv` writes it, hosted keys removed), `gemini` (the same three turns on gemini-3.5-flash-lite, key
  from GEMINI_API_KEY or gem.env, never printed, gateway retry only), `doctor` (the Local Model check on the
  9B with `agent.mode: solo` and `models.reasoning_effort: none` as setup writes them; 300 s per case so load
  is not scored as model weakness, each case's ms printed). The runner is built as runner-for-mode builds it:
  `soloTurnSettings({}, adapters)`, the real run meter, the real `file_ops` (local sandbox, writes
  auto-approved), the context window from `readContextWindow` on the local run.
- ~03:20 `npx tsc --noEmit -p apps/cli/tsconfig.json` -> exit 2: `ReplConfig has no properties in common with
  SoloTurnConfig` (weak type) at my two call sites, plus model-gateway/complete.ts (C14's lane, mid-edit).
  Fixed mine (`soloTurnSettings(config: unknown, ...)`, read loosely like `soloAgentSettings`) -> exit 0.
  `cd packages/trent-core && npm run build` -> exit 2, 4 errors, all in src/tools/memory/owner.test.ts
  (another lane's red test; tools/memory/** is outside this lane). No error in a C11 file.
  `runner-for-mode.constrained.test.ts` after the type change -> exit 0, 5/5.
- ~03:21 docs (not number-dependent): configuration.md "Agent mode" (the stale "no surface is wired to it yet"
  sentence replaced by a pointer to solo.md, S2 having landed; `agent.solo.max_tool_calls` documented),
  doctor.md (row 23, the solo-format smoke bullet, budget twelve minutes), solo.md ("Constrained output").
  Markdown hunks carry `<!-- [C11] -->` for `scripts/dev/hunks.py`. `docs-truth.test.ts` -> exit 0, 11/11.
  C15's lane is editing solo/prompt.ts meanwhile (stubs `SOLO_RULES`, `soloPlatformHint`); the names this
  lane calls (`soloResponseFormat`, `buildSystemPrompt`, `SOLO_TOOL_PROTOCOL`, `DEFAULT_SOLO_PERSONA`) are
  untouched.
- 03:22 gates (`uptime` 1-min load 6.51): `npx vitest run packages/trent-core/src/solo`
  -> exit 1, 21 files, 5 failed / 117 passed: all 5 in `prompt-default.test.ts`, C15's red test (its lane,
  in progress); the other 20 files pass. `packages/trent-core/src/config` -> exit 0, 15 files / 79 tests.
  `packages/trent-core/src/doctor` -> exit 0, 23 files / 165 tests. `apps/cli/src/runtime` -> exit 0, 15
  files / 98 tests.

## Live runs (one at a time, nothing else of mine running)
- 03:23:47 pre-flight: `uptime` load averages 58.93 33.80 64.51 (baseline load on this machine is high from
  unrelated processes; the brief says run anyway when no other vitest runs); `pgrep -c vitest` -> 0; Ollama
  0.32.9 at 127.0.0.1:11434, `/api/ps` empty (no model loaded: the first call pays the cold load),
  `qwen3.5:9b` pulled.
- 03:24:09 LIVE run 1, local (`uptime` 47.62 32.97 63.51 at start):
  `TRENT_TEST_LIVE=1 TRENT_QUEUE_FALLBACK=disabled npx vitest run packages/trent-core/src/solo/solo.live.test.ts -t "local: qwen"`
  -> exit 1 at 03:26:07 (load 149.15 73.31 75.21 at the end). The runner sent `response_format` json_schema
  `solo_turn`, window 32768 (Ollama's own figure). **0 tool calls in 3 turns; the assertion "turn 1 made no
  completed tool call" failed.** Transcript (every model reply, verbatim):
  - turn 1 "Read the file notes/brief.txt and tell me the delivery code written in it.": call 1 TTFT 26.9 s,
    933 in / 19 out, reply `{"name": "read_file", "arguments": {"path":"notes/brief.txt"}}` (the tool_call
    BODY, not the envelope; the parser refused it and asked once for a repair); call 2 TTFT 4.4 s, 1047 in /
    7 out, reply `"notes/brief.txt"` (a bare JSON string), taken as the answer. 38.4 s, 0 cents.
  - turn 2 "List the files in the notes directory.": call 1 TTFT 28.9 s, reply `{"name": "list_directory",
    "arguments": {"path": "notes"}}` (a name OUTSIDE the enum); call 2 TTFT 4.4 s, reply `"notes/brief.txt"`,
    taken as the answer. 39.8 s.
  - turn 3 "Write a two-line summary ... to summary.md": call 1 TTFT 29.7 s, 991 in / 23 out, reply
    `1.0748526684570312e+18` (a bare number), taken as the answer. summary.md NOT written.
  Diagnosis: the server did not decode under the solo schema. A name outside the enum, a bare string and a
  bare number are all impossible under it; they are exactly what a generic "any JSON value" grammar allows.
  `soloResponseFormat`'s schema is a top-level `oneOf` whose two branches carry only `required` (no `type`,
  no `properties`); llama.cpp's json-schema-to-grammar (which Ollama uses) visits `oneOf`/`anyOf` BEFORE
  `properties`, so the top-level properties (and the enum) are never read, and a branch with neither `type`
  nor `properties` is not an object rule. The L1 seat schema, which did constrain (2/5 -> 4/5), is a flat
  object with `properties` and `additionalProperties: false`. Two secondary faults the run exposed: a
  malformed reply's repair can come back as a bare JSON scalar that becomes the answer, and the solo prompt
  never names the envelope (it teaches `<tool_call>` blocks), so the model wrote the block's body.
- 03:27 probe (`scratchpad/probe1.json`, `probe2.json`; direct POSTs to Ollama's /v1/chat/completions, one
  call each, `reasoning_effort: none`, load 81.59 -> 152.47): the same envelope in three forms, prompt "List
  the files in the notes directory." `current` (soloResponseFormat's `oneOf`) -> `{"name": "search_files",
  "arguments": {"path": "notes"}}` (NOT decoded under the schema); `anyOf` of two complete objects ->
  `{"tool_calls": [{"name": "search_files", "arguments": {"path": "notes"}}]}`; flat object (both properties,
  none required) -> the same. Prompt "What is 17 + 25?": `anyOf` -> `{ "answer": "42" }`; flat -> `{  }`
  (nothing required, so nothing said). Decision: send the envelope as an `anyOf` of two complete objects,
  each with its one property required and `additionalProperties: false`, the call item and enum taken from
  `soloResponseFormat` itself. Not in prompt.ts (C15's file this wave): in turn-settings.ts, the one place
  the runtime and the doctor both read. The defect in `soloResponseFormat` is reported for the prompt.ts
  owner.
- 03:29 item 4 (from the live evidence) RED: new `packages/trent-core/src/solo/turn-settings.test.ts` -> exit 1,
  3 failed / 1 passed: `expected [ Array(2) ] to be undefined` (the top-level `oneOf` was sent), no `anyOf`.
  GREEN: `soloEnvelopeFormat(adapters)` in turn-settings.ts (the call item and the enum from
  `soloResponseFormat`, the envelope restated as the enforced `anyOf`; throws if the source loses either
  property), used by `soloTurnSettings` and by the doctor's solo format (`local-smoke-solo.ts`), so the doctor
  measures exactly what the runtime sends. turn-settings.test.ts -> exit 0, 4/4; local-smoke-solo.test.ts ->
  exit 0, 6/6 and runner-for-mode.constrained.test.ts -> exit 0, 5/5 (their enum helpers now read the
  `anyOf`'s call alternative). The fakes cannot see a grammar, so the shape itself is guarded by the unit test.
- 03:31 gates again after the fix: `npx vitest run packages/trent-core/src/solo` -> exit 0, 22 files / 126 tests
  (C15's prompt-default.test.ts now passes in its own lane); `packages/trent-core/src/doctor` -> exit 0, 23 / 165;
  `packages/trent-core/src/config` -> exit 0, 15 / 79; `apps/cli/src/runtime` -> exit 0, 15 / 98.
  `npx tsc --noEmit -p apps/cli/tsconfig.json` -> exit 0. `cd packages/trent-core && npm run build` -> exit 2,
  one error, in src/tools/memory/owner.test.ts (another lane's; none in a C11 file).
- 03:32:35 LIVE run 2, local, after the envelope fix (`uptime` 54.32 78.83 78.40 at start; model already
  loaded): same command. Every reply was now the envelope (`{"tool_calls":[{"name":"read_file","arguments":
  {"path":"notes/brief.txt"}}]}`): the schema was enforced. But turn 1 made the same completed `read_file`
  call 26 times and never answered, until "stopped at the solo loop's cap of 25 tool calls" (run_failed,
  266.6 s, 26 model calls, 52,858 tokens in / 521 out, first-call TTFT 22.4 s then about 3.5 s each, 0 cents).
  Cause: the model's only reply rule is the text protocol's ("reply in plain text with no <tool_call>
  block"), which the grammar makes impossible, and nothing names `{"answer": ...}`: L1's seats needed the same
  appended instruction (`CONSTRAINED_SEAT_INSTRUCTION`). I stopped the run at 03:38:54 during turn 2 (TaskStop;
  turns 2 and 3 would have repeated the loop, about 9 more minutes of the 9B, and told nothing new); raw output
  in scratchpad `live-local2.txt`. The misuse stop did not fire because it counts failed, refused or held
  calls, not repeated successful ones (a separate item, not taken here).
- Design for the instruction: C15 is rewriting `buildSystemPrompt` (a platform hint as the prefix's last
  section), so the prefix build in runner.ts is left alone. As L1's seat port does, the instruction is
  appended to the system message at request time, in turn.ts's one model call, only when the request carries
  a format: the stored conversation and the frozen prefix are unchanged, the suffix is constant (the prompt
  cache holds), compaction calls never carry a format. The doctor's solo smoke appends the same.
- 03:40 item 5 (from live run 2) RED: turn-settings.test.ts -> exit 1, 2 failed / 5 passed (a stub
  `SOLO_ENVELOPE_INSTRUCTION = ""`: `expected '' to contain '{"tool_calls": [{"name": '`, and the system message
  unchanged); runner-for-mode.constrained.test.ts -> exit 1, 1 failed / 4 passed (the local request's system
  message did not end with the instruction); local-smoke-solo.test.ts -> exit 1, 1 failed / 5 passed (same, the
  doctor's solo system prompt). GREEN: `SOLO_ENVELOPE_INSTRUCTION` and `withEnvelopeInstruction` in
  turn-settings.ts; turn.ts `[C11]` (import, and the one `gateway.complete` call sends
  `withEnvelopeInstruction(state.messages, deps.request.responseFormat)`); the doctor's `soloSmokeMessages`
  applies it. The three files -> exit 0 (7/7, 5/5, 6/6). Gates: solo -> exit 0, 22 / 129; doctor -> exit 0,
  23 / 165; runtime -> exit 0, 15 / 98; config -> exit 0, 15 / 79; `npx tsc --noEmit -p apps/cli/tsconfig.json`
  -> exit 0; `cd packages/trent-core && npm run build` -> exit 0 (the other lane's owner.test.ts has cleared).
- 03:49:36 LIVE run 3, local, after both fixes (`uptime` 41.99 94.78 118.53 at start; other lanes' vitest
  runs waited out first; the model was unloaded, so turn 1 paid the load): same command -> **exit 0** at
  03:53:04 (load 167.63 115.45 120.90 at the end). response_format json_schema `solo_turn` (the `anyOf`),
  window 32768. Transcript, verbatim replies:
  - turn 1 "Read the file notes/brief.txt and tell me the delivery code written in it." (64.0 s, run_done):
    call 1 TTFT 33.2 s, 39.6 s, 1043 in / 24 out: `{"tool_calls": [{"name": "read_file", "arguments":
    {"path":"notes/brief.txt"}}]}` -> read_file completed (`1|Order 7731 ships on Thursday. 2|The delivery code
    is PELICAN-42.`); call 2 TTFT 3.8 s, 24.4 s, 1135 in / 68 out: `{"answer": "The file notes/brief.txt
    contains the following: ... So, the delivery code written in it is **PELICAN-42**."}`. Right answer.
  - turn 2 "List the files in the notes directory." (57.3 s, run_done): call 1 TTFT 36.9 s, 46.0 s, 1212 in /
    30 out: `{"tool_calls": [{"name": "search_files", "arguments": {"pattern":"*.txt","target":"files",
    "path":"notes"}}]}` -> completed (`2 files match(es) for *.txt notes/brief.txt notes/suppliers.txt`);
    call 2 TTFT 3.6 s, 11.2 s, 1288 in / 24 out: `{"answer": "The notes directory contains two text files:
    1. brief.txt 2. suppliers.txt"}`. Right answer.
  - turn 3 "Write a two-line summary of what you have learned to summary.md in the workspace root." (77.9 s,
    run_done): call 1 TTFT 39.5 s, 61.5 s, 1334 in / 71 out: two calls in one reply, `write_file
    {"path":"summary.md","content":"The notes directory contains brief.txt and suppliers.txt.\nThe delivery code
    for Order 7731 is PELICAN-42."}` (completed, 105 bytes) and a redundant `read_file notes/brief.txt`
    (completed); call 2 TTFT 6.0 s, 16.0 s, 1502 in / 33 out: `{"answer": "I wrote a two-line summary to
    summary.md ..."}`. summary.md on disk: the two lines above.
  - Per turn tokens in/out: 2178/92, 2500/54, 2836/104 (session 7514 in / 250 out); cents 0 each (the gateway's
    and the meter's: a local call is priced at zero; list-price micro-cents 0). TTFT: the first call of each
    turn 33.2 / 36.9 / 39.5 s, the second 3.8 / 3.6 / 6.0 s. Observation, not investigated: a turn's opening is
    sent with its context tier but stored without it (runner.ts appends the bare objective), so the next turn's
    conversation is not a byte-extension of what the server last saw; with the load at 42-168 the first-call
    prefill is the dominant cost (a C12/C13 question).
- 03:55:39 LIVE doctor run (`uptime` 131.51 115.74 119.43 at start; no other vitest running):
  `TRENT_TEST_LIVE=1 TRENT_QUEUE_FALLBACK=disabled npx vitest run packages/trent-core/src/solo/solo.live.test.ts -t "doctor"`
  -> **exit 0** at 04:02:20 (load **796.32** 558.98 327.22 at the end: other lanes' work spiked it mid-run).
  The Local Model check on qwen3.5:9b, profile `agent.mode: solo`, `models.reasoning_effort: none`, 300 s per
  case: status warn.
  - **Solo-format smoke: 4/5.** call PASS 32.4 s; abstain PASS 28.6 s; escaping FAIL 46.6 s ("content arrived as
    `\"\"\"\nShe said \"yes\".\nThen she left.\"`": the JSON was valid, the model wrapped the content in Python
    triple quotes, the same content error L1's constrained seat made); required PASS 32.6 s; unknown-tool PASS
    32.1 s. Every case under the shipped 60 s deadline, so the 300 s setting did not change the score.
  - Seat format (unconstrained, the doctor's L0-4 smoke) in the same run: 1/5 (call, escaping, required,
    unknown-tool FAIL: `toolCall.action was not a "<tool> <json>" string`; abstain PASS), as L0-4 measured.
  - First token at a 4,110-token prompt: 130.3 s (under the load above). Context 32768 (`/api/ps`).
- 04:04:34 LIVE hosted run (`uptime` 287.55 485.23 334.92 at start; no other vitest running):
  `TRENT_TEST_LIVE=1 TRENT_QUEUE_FALLBACK=disabled npx vitest run packages/trent-core/src/solo/solo.live.test.ts -t "hosted: gemini"`
  -> **exit 0** at 04:04:45. gemini-3.5-flash-lite, key read from gem.env (the output was checked: no key
  pattern in it), one attempt, no 429. No response_format (the text protocol). Transcript, verbatim replies:
  - turn 1 (1.8 s, run_done): call 1 TTFT 0.97 s, 970 in / 33 out: `<tool_call>{"name": "read_file",
    "arguments": {"path": "notes/brief.txt"}}</tool_call>` -> completed; call 2 TTFT 0.64 s, 1067 in / 31 out:
    "I read `notes/brief.txt`, which showed that the delivery code is `PELICAN-42`. Nothing is left to do."
  - turn 2 (1.3 s, run_done): call 1 TTFT 0.53 s, 1108 in / 40 out: `search_files {"path": "notes", "pattern":
    "*", "target": "files"}` -> completed (2 files); call 2 TTFT 0.53 s, 1191 in / 36 out: "I searched the
    `notes` directory, which showed two files: `notes/brief.txt` and `notes/suppliers.txt`. ..."
  - turn 3 (1.8 s, run_done): call 1 TTFT 0.79 s, 1249 in / 71 out: `write_file {"path": "summary.md",
    "content": "The delivery code in notes/brief.txt is PELICAN-42.\nThe notes directory contains two files:
    notes/brief.txt and notes/suppliers.txt."}` -> completed (132 bytes); call 2 TTFT 0.56 s, 1354 in / 82 out:
    "I wrote the two-line summary to `summary.md`. What I did: ... What it showed: ... What is left: ..."
  - Tokens in/out per turn: 2037/64, 2299/76, 2603/153 (session 6939 / 293). Cents: the meter charged 1 per
    turn (3 for the session: it sums a run's micro-cents and rounds once, P2-8); the gateway's per-call
    figure is 1 per call (6, each call rounded up); the list price is 77,110 / 87,970 / 116,340 micro-cents per
    turn, 281,420 for the session = 0.28 cents.
- Prompt provenance: C15's solo system prompt was committed at 03:48:26 (2d95444), so local run 3, the doctor
  run and the hosted run all used it; runs 1 and 2 used the prompt before it. HEAD is 6202bc4 at 04:05.
- 04:08 docs with the measured numbers: local-models.md (the stale "S2 landing now" sentence corrected, a
  solo-format row in "What to expect", a "Solo on this machine" subsection, the doctor paragraph, the
  tool-call-format limit), solo.md (the constrained-output paragraph completed; the "not landed" limit
  replaced by the live proof). `docs-truth.test.ts` -> exit 0, 11/11; `docs-truth-pages.test.ts` -> exit 0, 11/11.
- 04:09 final: `npx tsc --noEmit -p apps/cli/tsconfig.json` -> exit 0; `cd packages/trent-core && npm run build`
  -> exit 0; `node scripts/ci/repo-scan.mjs` -> exit 0; `npx vitest run packages/trent-core/src/solo` -> exit 0,
  22 files / 133 tests; `apps/cli/src/runtime` -> exit 0, 15 / 98; `packages/trent-core/src/config` -> exit 0,
  15 / 79; `packages/trent-core/src/doctor` -> exit 0, 23 / 165. Every `-U0` hunk of every existing file this
  lane touched carries `[C11]` (checked with the same hunk split `scripts/dev/hunks.py` uses).

## Result
- The 9B solo-format smoke score: **4/5** (section 7 item 4 names 4/5 or better as the bar; this lane reports
  and does not decide the default).
- Local 3-turn session on qwen3.5:9b: pass (run 3), 3/3 turns with a completed tool call, 0 cents.
- Hosted 3-turn session on gemini-3.5-flash-lite: pass, 3/3, 3 cents charged (0.28 list).

## Files
New: `packages/trent-core/src/solo/turn-settings.ts`, `solo/turn-settings.test.ts`, `solo/solo.live.test.ts`,
`config/agent-solo-schema.test.ts`, `doctor/checks/local-smoke-solo.ts`, `doctor/checks/local-smoke-solo.test.ts`,
`apps/cli/src/runtime/runner-for-mode.constrained.test.ts`, this log.
Changed (`[C11]` hunks): `config/sections/agent.ts`, `apps/cli/src/runtime/runner-for-mode.ts`, `solo/turn.ts`,
`doctor/checks/{local-model,local-smoke,local-stream}.ts`, `doctor/checks/local-runtime.test-helpers.ts`,
`doctor/checks/local-model.test.ts`, `docs/{configuration,doctor,solo,local-models}.md`. In
docs/configuration.md only the "Agent mode" hunk is this lane's; the `trent improve sweep --live` hunk near line
1047 is another lane's.

## Open (not done in this lane)
1. `soloResponseFormat` (solo/prompt.ts, not edited here) still states the envelope as a top-level `oneOf`
   that Ollama does not enforce; it is now only the source of the call item and the enum, and
   `soloEnvelopeFormat` restates it. The prompt.ts owner should restate it as the `anyOf` (turn-settings.ts
   then reduces to calling it).
2. A turn's opening is sent with its context tier but stored without it, so each turn's first call re-prefills
   the conversation (33-40 s TTFT under this load, against 3.6-6.0 s for the second call).
3. The misuse stop counts failed, refused and held calls only: run 2 repeated one successful call 26 times up
   to the cap. A repeat-of-a-success guard is its own item.
4. `repairPrompt` (turn.ts) still teaches `<tool_call>` blocks under constrained output (the grammar makes a
   malformed envelope unlikely; not observed in the passing runs).
5. docs/local-models.md "The app's job timeout ... Sizing it through `models.local` is planned for the next wave
   (L1), not built" is stale (L1 built `models.local.job_timeout_seconds`); left for its owner.
6. Nothing committed, staged or stashed.
