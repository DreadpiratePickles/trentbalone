# 2026-09-25 — P2-5a truth bundle

Agent: Opus subagent (no sub-subagents). Branch `feature/trent-fleet-v2`, HEAD `4a0e171`.
Scope: nine truth defects from `02_plan/output/hermes-parity-scorecard-2026-09-25.md` §4 and the
"For P2-5" lines in `docs/sessions/2026-09-25-parity-push.md`. No commits, no stash, no push.
Owned files only; other agents edit gateway/fleet/orchestrator/fleet-memory/service/setup/improve/brain/config.
Env for every vitest run: `TRENT_QUEUE_FALLBACK=disabled`, run from repo root.

## Log
- Read rulebook, AGENTS.md, CONTEXT.md, scorecard §4, parity-push "For P2-5" lines.
- Baseline `npx vitest run packages/trent-core/src/tools` -> exit 0 (38 files, 290 passed, 19 skipped).
- Item 1 (reserved names). RED: new `tools/tool-names.test.ts` builds every adapter (all
  IMPLEMENTED_TOOLSETS + always-on + bridge `deferred()` + memory/fleet_search/brain_read) ->
  exit 1, missing: social, social_platforms_list, social_post, social_reply, social_inbox_list,
  social_insights_read, social_schedule, fleet_skill_view, brain_read; a plugin claiming
  social_post/brain_read/fleet_skill_view loaded. The old check in tools-index.test.ts only read
  advertised scopes, and disclosure had deferred every social name (as "foreign"), so it never saw them.
  GREEN: `tool-names.ts` derives every entry from the registries' constants (scopes / adapter name +
  schema names); `plugins/names.ts` and `tool_search/names.ts` are new leaf modules (re-exported
  from their index.ts) so the derivation has no import cycle (checked with a static reach script).
  `npx vitest run packages/trent-core/src/tools` -> exit 0 (39 files, 314 passed).
  Side effect, intended: social tools are no longer "foreign" to disclosure, so they are deferred
  only past the threshold like every other built-in.
- Not mine: apps/cli mcp/mcp-serve tests fail to load because `commands/groups/run.ts` imports
  `runtime/child-run.js`, which another agent (P2-1) has not written yet.
- Item 2 (mediaImagePresent). Reproduction on this machine (Docker client/server 29.5.3, API 1.54,
  overlay2 store, image created 2026-09-20): `docker inspect --type image --format {{.Id}}
  trent-sandbox-media:1` -> exit 0 (sha256:99d8c656...); `docker image ls --filter
  reference=trent-sandbox-media:1` -> exit 0 (lists 99d8c656aec6); `docker image inspect` -> exit 0.
  Same with `env -i PATH=...` (with and without HOME). So the P2-3 false negative does NOT
  reproduce now. Both inspect forms hit the same endpoint (GET /images/{name}/json); an earlier
  session (doctor/checks/workbench.ts comment) recorded the mirror-image claim. Likely transient.
  Fix anyway, since `auto` silently choosing the host is the harm: inspect first, then
  `docker image ls --filter reference=<ref> --format {{.ID}}` (a different endpoint, Docker 1.13+)
  before answering absent. RED: new `tools/media/backend.test.ts`, 3 failed / 2 passed (fake daemon
  answering "No such image" to inspect and listing the image -> false). GREEN: `npx vitest run
  packages/trent-core/src/tools/media` -> exit 0 (3 files, 24 passed); media.test.ts's absent-probe
  assertion now states both calls.
- Item 3 ("needs Bun" causes). The messages live at `apps/cli/src/commands/improve-sweep.ts:161`
  and `apps/cli/src/repl/index.ts:299` (no `packages/trent-core/src/improve/improve-sweep.ts`).
  Real error shapes captured: Node importing `bun:` -> ERR_UNSUPPORTED_ESM_URL_SCHEME; Bun 1.4.2
  importing a missing `./generated/client` -> ResolveMessage ERR_MODULE_NOT_FOUND "Cannot find
  module './generated/client' imported from ...". RED: `store/durability.test.ts` (module absent),
  `commands/__tests__/store-durability.test.ts` 2 failed (reason "bun:sqlite unavailable ..." under
  both runtimes), `repl/__tests__/durability.test.ts` 2 failed (Bun boot said "needs Bun"; Node boot
  gave no way forward). GREEN: new dependency-free `packages/trent-core/src/store/durability.ts`
  (`explainStoreFailure`, `diagnoseStoreFailure`: needs_bun / client_not_generated / open_failed);
  both surfaces call it -> 3 files, 11 passed. Real runs (throwaway HOME): Node `npm run cli --
  improve status --json` -> exit 0, reason "the SQLite store needs Bun (...)"; Bun with TRENT_HOME
  not yet created -> exit 0, "the SQLite store did not open: unable to open database file" (the old
  text would have said bun:sqlite unavailable); Bun with TRENT_HOME created -> `{"durable":true}`.
  Found, not fixed (not my file beyond the message): `improve` never creates a missing profile dir.
- Item 4 (doctor hint). CLI sets the variable only when unset/empty (`apps/cli/src/env-defaults.ts:16`).
  RED: `doctor/checks/environment.test.ts` 3 failed / 4 passed (hint was the fixed "Export
  TRENT_QUEUE_FALLBACK=disabled and unset ..." for every violation). GREEN: `fixHintFor(violations)`
  names only the wrong variables; queue: "Unset TRENT_QUEUE_FALLBACK or set it to disabled (the CLI
  sets disabled itself when it is unset)" -> 7 passed. `docs/doctor.md:32` keeps the old line inside
  a dated verbatim transcript that already says the hint changed; left as is.
- Item 5 (completion-port key names). The gateway counts Google as configured by GOOGLE_API_KEY or
  GEMINI_API_KEY (apps/web/lib/ai-client.ts:84) and reaches DeepSeek/Groq through the `provider:`
  alias setup writes, so setup's `PROVIDER_ENV_VARS` (setup/detect.ts, read-only import; imports
  only fs, dotenv and types, no cycle) is the right single table. RED: new test in
  completion-port.test.ts -> 1 failed / 5 passed (message named 5, setup detects 8: DEEPSEEK,
  GOOGLE, GROQ missing). GREEN: NOT_CONFIGURED_MESSAGE built from the table -> 6 passed; still
  contains "not configured" (isNotConfiguredError).
- Item 6 (getting-started toolsets). ToolsetSchema has 16 names; DEFAULT_CONFIG.toolsets has 9, so
  "everything except browser, vision, memory and mcp" was stale too (media, social, business).
  RED: new docs-truth describe "the documents state the toolsets the schema accepts" (reads the
  count word, the listed names, the default count and the off-by-default names from ToolsetSchema
  and DEFAULT_CONFIG) -> `"accepts thirteen names": expected 13 to be 16`. GREEN after the one
  paragraph edit (docs/getting-started.md ~125; another agent's concurrent edits are in the run
  section, untouched). README.md:171 still says thirteen: reported, and README is left out of
  TOOLSET_PAGES until its owner fixes it.
  Pre-existing, not mine: docs-truth "states the command counts" fails [34,146] vs [35,151] (README).
- Item 9 (/exit). Session end was Ctrl+D (`eof` -> deps.exit(0), ignored while busy) or stdin
  closing; no /quit in the REPL (the TUI has /exit and /quit). commands.ts and engine.ts were both
  at 499 lines. RED: new `repl/__tests__/exit.test.ts` 5 failed (Unknown command: /exit; the real
  REPL boot never exited), docs-truth "resolves every documented slash command" failed on the
  newly documented `/exit` in getting-started. GREEN: `ReplSessionControl` port on ReplContext
  (types.ts, session-view.ts ports), wired in repl/index.ts to the same `exit` Ctrl+D uses and
  refused while `engine.busy`; `/exit` added last in REPL_COMMANDS; `/checkpoints` + `/rollback`
  moved unchanged to new `repl/checkpoint-commands.ts` (verified identical to HEAD modulo run()
  annotations) so commands.ts is 418 lines; commands.test.ts exempts `exit` from the
  "output changes with state" loop like `help`. `npx tsc --noEmit -p apps/cli/tsconfig.json` -> 0;
  `npx vitest run apps/cli/src/repl apps/cli/src/commands/__tests__/docs-truth.test.ts` -> 31 files
  passed, 1 failed (only the pre-existing README command count).
- Item 7 (AGENTS.md defect 9). Evidence per item, run now: `npx vitest run
  tools/skills/skills.test.ts skills/skill-store.test.ts fleet-memory/memory-draft.test.ts
  fleet-memory/config.test.ts fleet-memory/per-seat-prelude.test.ts doctor/checks/egress-ca.test.ts`
  -> exit 0, 6 files, 40 tests. Commits by `git log -S`: one skill store 5f9e149; lock bypass and
  dead recall reader 9f63c7a; per-seat prelude e7bfd4e; egress root doctor check + --fix 4f9f3f4.
  `grep TRENT_FLEET_RECALL_BUDGET_CHARS` outside tests/README/comments -> no match. Defect 9 now
  says: still open (Node EphemeralStore, `openStore`), mitigated (egress root: doctor names it,
  --fix deletes it), fixed (the four, each with its commit and test name).
- Item 8 (CONTEXT.md). "current design" -> upgrade-round-design.md (v2, gate decisions 2026-09-20
  in docs/sessions/2026-09-19-upgrade-round.md) + the scorecard; "task list" -> parity-push.md
  (Wave P2 plan) ranked by scorecard §2. Note: the scorecard and parity-push.md are still untracked.

## Verification (TRENT_QUEUE_FALLBACK=disabled, repo root)
- `npx vitest run packages/trent-core/src/tools packages/trent-core/src/doctor
  packages/trent-core/src/model-gateway/completion-port.test.ts packages/trent-core/src/setup
  apps/cli/src/repl apps/cli/src/commands/__tests__/docs-truth.test.ts
  packages/trent-core/src/wrapped-modules.test.ts` -> EXIT=1, 94 files passed / 1 failed,
  697 tests passed / 1 failed. The one failure is docs-truth "states the command counts":
  README.md:75 says 34/146, the registry has 35/151 (README owner; P2-3 logged the same today).
- Extra suites my changes touch: store/durability, commands/store-durability, commands/improve,
  mcp-server, fleet/pack-skills, fleet/import-foreign, commands/mcp, commands/mcp-serve,
  model-gateway -> EXIT=0, 23 files, 198 tests.
- `cd packages/trent-core && npm run build` -> 0. `npx tsc --noEmit -p apps/cli/tsconfig.json` -> 0.
  `node scripts/ci/repo-scan.mjs` -> 0 (all PASS).
- Real daemon, new probe under Bun on the source: trent-sandbox-media:1 -> true (inspect exit 0,
  one call); :nope -> false (inspect exit 1, image ls exit 0 empty); `auto` -> docker.
- Every touched file under 500 lines (largest: tool_search/index.ts 433, repl/commands.ts 418).

## For the README owner (P2-B)
- README.md:75 `34 commands, 146 with their subcommands` -> the registry's count (35/151 now).
- README.md:111 add `/exit` (or Ctrl+D) ends the session once nothing is running.
- README.md:164 memory row: add `brain_read` (the hook registers it; now reserved too).
- README.md:171-173 `accepts thirteen names ... writes all thirteen` -> sixteen, and quick setup
  adds media/social/business only when a backend or provider exists (setup/QuickSetup.ts). Phrase it
  as getting-started does ("accepts sixteen names: `...`." and "no `toolsets` key gets nine of them
  — everything except `...`.") and add README.md to TOOLSET_PAGES in docs-truth.test.ts.
- README.md:153-169 Tools table has no media, social or business rows (scorecard §4.1).
- README.md:160 "names cannot shadow built-ins" is now true; keep it.
- README.md:33/83 the export line and "must be exported" (P2-A1 already reported): the CLI sets it;
  the doctor now says "Unset TRENT_QUEUE_FALLBACK or set it to disabled" for an explicit bad value.

## Not changed, for the lead
- `improve` under Bun on a TRENT_HOME that does not exist yet opens no store ("did not open: unable
  to open database file"); nothing creates the profile dir on that path.
- docs/troubleshooting.md:177 quotes the Node line; still accurate, the Bun line is new.
- The TUI has `/exit` and `/quit` (tui/App.tsx:206, `process.exit(0)` without the REPL's cleanup).
