# 2026-09-26 — L0-3: local-model setup, the DEGRADED banner, the local judge

Agent L0-3 (Opus), wave L0 of `02_plan/output/local-models-plan-2026-09-26.md`. Gaps owned: G3, G15
(setup needs no key for a local runtime), G7 (DEGRADED means no usable provider), G6 (the improve judge
never falls back to a hosted model under a local provider), plus the tiered default model and the honest
expectation in setup and `docs/getting-started.md`. Branch `feature/trent-fleet-v2`, HEAD `412f5b0`.
Other L0 agents edit the tree concurrently; this agent does not commit, stash, checkout or push.

Owned: `packages/trent-core/src/setup/**`, `apps/cli/src/repl/degraded.ts` (+ tests),
`packages/trent-core/src/improve/judge-model.ts` (+ test), `docs/getting-started.md` ("Local models"),
this log.

## Model pull (the one pull this wave is allowed)

- `ollama search` does not exist in Ollama 0.32.9 (`Error: unknown command "search"`), so the tag was read
  from https://ollama.com/library/qwen3.5/tags (fetched 2026-09-26): `qwen3.5:9b` is `latest` for the 9B,
  digest `6488c96fa5fa`, 6.6 GB, the same digest as `qwen3.5:9b-q4_K_M`.
- **Pulled:** `ollama pull qwen3.5:9b` — exit 0, real 96.32 s, 2026-09-26T01:39:08Z to 01:40:44Z (UTC).
  `ollama list`: `qwen3.5:9b  6488c96fa5fa  6.6 GB`. `ollama show`: qwen35, 9.7B, Q4_K_M, context 262144,
  capabilities completion, vision, tools, thinking.
- **Deviation, recorded:** the brief said "under 6 GB". Ollama's Q4_K_M package is 6.6 GB because it
  carries the vision projector; the 5.7 GB figure in the plan is the text-only Hugging Face GGUF. A 3.5 9B
  tag exists, so the brief's first preference applies; no other tag was pulled. Other agents (L0-2, L0-4,
  L0-5) use `qwen3.5:9b`.

## Log

### Baseline (before any change)
`npx vitest run packages/trent-core/src/setup apps/cli/src/repl/__tests__/degraded.test.ts packages/trent-core/src/improve/judge-model.test.ts`
→ 4 files, 48 tests passed, exit 0.

### RED (tests written first; helper modules `local-tiers.ts` and `local-runtime.ts` added so the
wizard tests fail on behaviour rather than on a missing import)
- G6, `improve/judge-model.test.ts` "[L0-3] the judge under a local provider": 3 failed.
  `resolveJudgeModel({executor:"qwen3.5:9b", provider:"ollama"})` returned a priced Gemini id
  (`ollama: expected false to be true`: no TrentError thrown); the planner-tier and hosted-configured
  cases `expected [Function] to throw an error`.
- G3, `setup/local-setup.test.ts` quick `--provider ollama`: `AssertionError: No key found for ollama.
  Set OLLAMA_API_KEY in your environment or in <tmp>/.env, then run setup again.: expected false to
  be true`; runtime-down and not-pulled cases `expected 'no-key' to be 'runtime-unreachable'` /
  `'model-not-pulled'`; lmstudio `No key found for lmstudio. Set LMSTUDIO_API_KEY`.
- G15, full setup picking ollama: `expected 'Model provider\nModel\n…' not to match /already set/`.
- Default: `expected 'llama3.2' not to match /llama3\.2/`. Docs: `expected '# Getting started …' to
  match /## \d+\. Local models/`.
- G7, `repl/__tests__/degraded.test.ts`: `isDegraded({}, "ollama")` → `expected true to be false`;
  `degradedState` not a function (4 cases). Total red: 23 failed in the two files, 3 in the judge.

### GREEN (implementation)
- New `packages/trent-core/src/setup/local-tiers.ts` (tiers from local-models §6; tags read from
  ollama.com 2026-09-26: 16 GB `qwen3.5:9b` 6.6 GB, 32 GB `qwen3.6:27b` 18 GB, 64 GB
  `qwen3.6:35b-a3b` 23 GB; three-sentence expectation each). `qwen3.6:27b` over `qwen3.8:27b` for
  the 32 GB tier: Qwen3.6-27B's card documents a tool parser (`qwen3_coder`), Qwen3.8-27B's names none.
- New `setup/local-runtime.ts` (probe `/api/version` + `/api/tags`, LM Studio `/v1/models`; pull via
  `POST /api/pull` NDJSON; base URL from the gateway's `aliasBaseUrl`, so `OLLAMA_BASE_URL` is honoured).
- New `setup/local-setup.ts` (quick: runtime-unreachable / model-not-pulled stops, `--pull` behind a
  confirmation; full/blank-slate: reported, never blocking).
- `QuickSetup.ts` local branch + shared `finish()`; `FullSetup.ts` no key step for a local provider,
  tier default; `BlankSlate.ts` tier default; `detect.ts` `DEFAULT_MODELS.ollama` `qwen3.5:9b` and a
  guidance line for keyless local setup; `types.ts` two reasons, `pull`, `localRuntime`,
  `totalMemoryBytes`; `SetupWizard.ts` defaults.
- G7: `apps/cli/src/repl/degraded.ts` `isDegraded(source, provider)` + `degradedState()` +
  `renderDegradedBanner(theme, width, notice?)`. Outside my list, minimal and necessary:
  `repl/index.ts` (call site, 2 lines), `repl/engine.ts` (one optional dep `degradedNotice`, the banner
  call passes it; the duplicate `../ui/index.js` import merged so the file stays at 499 lines),
  `repl/__tests__/harness.ts` (passthrough).
- G6: `improve/judge-model.ts` `provider` input; a local provider needs `improve.judge_model`, and
  refuses a priced or `:cloud` id. Caller `apps/cli/src/commands/improve-sweep.ts` passes
  `provider: cfg.provider` (one line).
- CLI: `--pull` on `trent setup` (`commands/groups/diagnostics.ts`, 2 lines), red first in new
  `commands/__tests__/setup-local.test.ts` (`expected 2 to be +0`: Commander refused the flag).
- TUI model modal label said "llama3.2" for a default that is now `qwen3.5:9b`; label corrected
  (`apps/cli/src/tui/modals/ModelModal.tsx`, one string).
- After: setup (3 files) + degraded + judge → 79 tests, 1 failing (docs section not yet written).

### Live run, first attempt (blocked by another agent's unlanded work)
Runner `T` = `env -i` scratch HOME/TRENT_HOME, `TRENT_QUEUE_FALLBACK=disabled`, under
`sandbox-exec` with outbound network denied except localhost (`curl` to api.mistral.ai under it: exit 7;
to 127.0.0.1:11434: exit 0). `T setup --mode quick --provider ollama --json` → exit 1,
`ERR_MODULE_NOT_FOUND .../doctor/checks/local-model.js imported from .../doctor/DoctorRunner.ts`:
L0-4's DoctorRunner import landed before its file. Not mine to fix; retry later.

### Real-runtime probe while the CLI import was blocked
`tsx probe-setup.mts` (scratchpad; env -i, sandbox localhost-only) → exit 0:
`{"reachable":true,"provider":"ollama","url":"http://127.0.0.1:11434","version":"0.32.9","models":["qwen3-embedding:0.6b","qwen3.5:9b",…]}`,
LM Studio `{"reachable":false,…,"error":"connection refused"}`, `os.totalmem()` 34359738368 → tier
`32gb` → recommended `qwen3.6:27b`. Note: `qwen3-embedding:0.6b` appeared in `ollama list` during
this session; L0-3 did not pull it (the only L0-3 pull is `qwen3.5:9b`).

### Docs
`docs/getting-started.md`: section 5 (quick on a local provider, the two new reasons), section 7
(no no-key paragraph for a local provider), new "## 11. Local models" (commands, tier table, the
three-sentence expectation per tier, the stops, the one-line DEGRADED notice, the judge rule).
`local-setup.test.ts` asserts every tier's tag and expectation appear verbatim. Two line-local truth
fixes outside my list: `docs/improve.md` (judge order under a local provider) and
`docs/configuration.md` (the judge sentence; the ollama row's default was `llama3.2`).
`npx tsc --noEmit -p apps/cli/tsconfig.json` → exit 2, only 2 errors, both L0-4's missing
`doctor/checks/local-model.js`; none in L0-3 files. `node scripts/ci/repo-scan.mjs` → exit 0.

### LIVE (no cloud: every command `env -i`, scratch HOME/TRENT_HOME, under `sandbox-exec` denying
all outbound network except localhost)
1. `T setup --mode quick --provider ollama --json </dev/null` (32 GiB machine, no `--model`) → **exit 3**,
   stdout one document: `{"mode":"quick","success":false,"message":"qwen3.6:27b is not pulled into Ollama at
   http://127.0.0.1:11434. Run: ollama pull qwen3.6:27b (or run setup again with --pull), then run setup
   again.","reason":"model-not-pulled","secretsConfigured":[]}`; stderr: "Ollama runs on this machine and
   needs no API key." / "Ollama 0.32.9 is answering at … Models it has: …" / "This machine has 32 GB of
   memory, so the recommended model is qwen3.6:27b (18 GB download)." / "  ollama pull qwen3.6:27b".
2. `T setup --mode quick --provider ollama --model qwen3.5:9b --json` under `script` (a pty; Enter
   answered the one confirmation) → **exit 0**: `{"mode":"quick","success":true,"message":"Quick setup
   complete. Provider ollama, model qwen3.5:9b, 3 starter agents, config at <scratch>/trent-home/config.yaml.",
   "secretsConfigured":[]}`; stderr carried "What to expect: Single tool calls mostly work at 9B. …".
3. `printf '/help\r' | T` → exit 0, `grep -c DEGRADED` = **0**; boot, tools line, /help list.
4. Runtime down (`OLLAMA_BASE_URL=http://127.0.0.1:9/v1`), `printf '/status\r' | …` → exit 0:
   `◆ DEGRADED — Ollama not answering at http://127.0.0.1:9. Run: ollama serve` and `Mode DEGRADED`.
   First attempt wrapped at 80 columns ("DEGRADED MODE — … Start it with:" / "ollama serve"); the test
   was tightened to 80 columns (red: `expected [ …(2) ] to have a length of 1 but got 2`) and the
   wording shortened (green).
5. Model not pulled (profile `model: qwen3.6:27b`) → `◆ DEGRADED — Ollama at http://127.0.0.1:11434 has
   no qwen3.6:27b. Run: ollama pull qwen3.6:27b` (wraps at 80 because of the tag length).
6. `T run "Say the word ready" --json` → **exit 1**, real 986.66 s:
   `{"type":"result","status":"failed","cost_cents":0,"duration_ms":977624,"run_id":"orc_p4uh865ztqm3",
   "model":"qwen3.5:9b","models":[],"error":"the run stopped on an error: Job job_sden7fi01kw0 timed out
   after 600000ms; 0 of 2 steps completed; consolidation skipped","reason":"run_error","failed_steps":[],
   "completed_steps":0,"total_steps":2,"consolidation":"skipped"}`.
   Ollama's log: planner `POST /v1/chat/completions` 200 in 6m4s (22:01→22:07), seat 200 in 9m57s
   (22:07→22:17), i.e. the calls reached the local model (not a 404 for `gpt-5.2`) but were cut by the
   app's 600 s job timeout. Gap: **G2's family** (timeouts not sized for local generation; here the
   worker job timeout, not the 60 s header timeout) compounded by **G18** (thinking on by default).
   Measured directly (`/api/generate`, "Say the word ready"): default thinking 201 tokens + 871
   thinking chars at 2.7 tok/s, 92 s; `think:false` 2 tokens, 87 s. The machine was shared with
   L0-2/L0-4/L0-5's live work (load average 467 at 22:28), so these speeds are contention, not the 9B's
   capability. `wiki-embeddings: … 404 model "text-embedding-3-small"` on stderr is G10 (L0-5).

### VERIFY (final, from the repo root, `TRENT_QUEUE_FALLBACK=disabled`)
- `npx vitest run packages/trent-core/src/setup apps/cli/src/repl packages/trent-core/src/improve/judge-model.test.ts
  apps/cli/src/commands/__tests__/behaviour.test.ts apps/cli/src/commands/__tests__/docs-truth.test.ts
  packages/trent-core/src/wrapped-modules.test.ts apps/cli/src/commands/__tests__/setup-local.test.ts`
  → 39 files, 330 tests, **exit 0**. (An earlier pass under load average ~467 timed out 9 REPL boot
  tests at 15-30 s and hit another agent's `/props` line in docs/configuration.md; rerun quiet: REPL
  31 files / 216 tests exit 0, and docs-truth passes once that line was fixed by its owner.)
- `cd packages/trent-core && npm run build` → **exit 0**.
- `npx tsc --noEmit -p apps/cli/tsconfig.json` → **exit 2**, one error, not L0-3's:
  `packages/trent-core/src/model-gateway/local-runtime.ts(158,64): TS2339 Property 'entries' does not
  exist on type 'Headers'` (model-gateway/** is L0-1/L0-2's). No error in any L0-3 file.
- `node scripts/ci/repo-scan.mjs` → **exit 0** (3 × PASS, 0 violations).
- Live: `T config set improve.judge_model qwen3.6:27b --json` exit 0, `config get` → `qwen3.6:27b` exit 0.
- Every L0-3 file under 500 lines (engine.ts 499, unchanged count).

### Left for others / not done
- `model-gateway/providers.ts:58` still has `defaultModel: "llama3.2"` (used only when a config's model
  is empty); model-gateway is L0-1/L0-2's.
- The live run needs local-sized timeouts beyond the 60 s header (the app's 600 s job timeout) and
  thinking control (G18) — L1 / L0-2.
