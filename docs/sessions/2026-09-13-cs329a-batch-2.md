# 2026-09-13 — CS329A batch 2: tasks I.6-I.12 + run-path wiring

Branch `feature/trent-fleet-v2`. Plan: `01_discovery/references/cs329a-applied.md` §3, §4, §6. Not committed.

## RED first (verbatim failures)
- I.6 `suites.test.ts`: `expected [ { type: 'llm_rubric', …(2) } ] to deep equally contain { type: 'contains', weight: 1, …(1) }`; `expected 'judge' to be 'deterministic'`
- I.7 `gate.test.ts`: `expected 1 to be 0.5` (fixture score; unverified evidence still counted as a pass)
- I.8 `lifecycle.test.ts`: `expected undefined to be false` (`judgeAgreement` missing)
- I.9 `gate.test.ts`: `expected { rubric_failed: 1 } to deeply equal { 'rubric_failed:two:a': 1 }` (one frontier bucket)
- I.10 `gepa-pass.test.ts`: `expected undefined to be 'suite_saturated'`
- I.11 `gepa-pass.test.ts`: `expected undefined to be 'proposal_too_long'`
- I.12 `gate.test.ts`: `expected true to be false` (promoted on one draw); `expected undefined to deeply equal [ 'r:1' ]`
- Wiring `apps/cli/.../improve.test.ts`: `TypeError: (0 , createImproveRunDeps) is not a function`

## GREEN
- gate.ts split: `gate-types.ts`, `gate-score.ts` (evidence check, per-fixture tags), `gate-redraw.ts` (I.12), `judge.ts` (`createGatewayJudge`, evidence contract).
- `mechanical-overlay.ts` + `overlays/ads/evals/mechanical.json`; `fileSuiteProvider(..., { overlayRoot })`.
- `ledger.ts:judgeAgreementFor`, `SkillLedgerRow.judgeAgreement` (StorePort.ts, additive), sqlite column; `status.judgeAgreement`, `status.suiteSaturated`.
- `gepa-pass.ts`: baseline before reflection; `suite_saturated` and `proposal_too_long` skips write an iteration row and remember the input hash.
- `sweep-baseline.ts` split out; baseline cache schema v2 carries per-fixture pass/fail.
- CLI: `createImproveRunDeps`, `fallbackImproveStore`, live judge on `--live`, overlay root, status render; `apps/cli/src/repl/improve-loop.ts` spread into `createOrchestrator`.

## Evidence
- `npx vitest run packages/trent-core/src/improve apps/cli/src/commands/__tests__/improve.test.ts` -> exit 0, 12 files, 69 tests.
- `npx tsc --noEmit -p packages/trent-core/tsconfig.json` -> exit 0.
- `TRENT_TEST_LIVE=1 npx vitest run .../improve.live.test.ts` -> exit 0; 5 real Gemini calls (sweep 1 = 2, sweep 2 = 1, judge 2).

## Notes
- Existing sweep tests had saturated baselines (1.0); under I.10 those now skip GEPA, so the scripted models were changed to fail one fixture at baseline (counts +1 for the re-draw).
- The length guard applies to model proposals only; the offline `<!-- gepa evolved -->` marker is a placeholder, not a proposal.
- Pre-existing, not mine: `apps/cli/src/tui/App.tsx(235)` `fixAll` type error; `desktop.test.ts` DMG install ECONNRESET.
