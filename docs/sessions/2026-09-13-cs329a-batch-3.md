# 2026-09-13 — CS329A batch 3: tasks I.13-I.16

Branch `feature/trent-fleet-v2`. Plan: `01_discovery/references/cs329a-applied.md` §1.5, §1.6, §1.8, §4 items 6 and 10, §6 I.13-I.16. Not committed.
Scope: `packages/trent-core/src/improve/**`, `apps/cli/src/commands/improve.ts`.

## Starting state
- `npx vitest run packages/trent-core/src/improve apps/cli/src/commands/__tests__/improve.test.ts` -> exit 0, 12 files, 69 tests.

## RED first (verbatim failures)
(filled in as each RED runs)
- I.13 `sweep.test.ts`: `AssertionError: expected 3 to be 2` (draft lists the retried step); `clean-trace.test.ts`: `Cannot find module './clean-trace.js'`; `lifecycle.test.ts`: `TypeError: (0 , createMemoryExemplarStore) is not a function`
- I.14 `rationalise.test.ts`: `Cannot find module './clean-trace.js'` (rationalise.js absent too)
- I.15 `repetitive-loop.test.ts`: `Cannot find module './repetitive-loop.js'`; `sweep.test.ts`: `expected +0 to be 1` (`skillsRejected`; no loop block)
- I.16 `suite-split.test.ts`: `Cannot find module './suite-split.js'`

## GREEN
- I.13 `clean-trace.ts`: `RawTrace` -> `CleanGolden` (ok steps only, `distilledFrom`, `candidateId`); blocked or unclean-outcome traces distil to nothing. `rawTraceFromRows` builds one raw trace per run (retry/replan/escalate or a loop tag = not ok; a `blocked` row blocks). `lifecycle.ts:distillExemplars` runs on every human `promoteDraft` with `distill: { exemplars, rationalise?, meter? }`. `sweep.ts`: triggers decided on the whole group, the Foundry handed `cleanTraces()` only (`toolCallThreshold: 0` on that call because the decision is already made); a group with nothing clean writes `blockedBy: "no_clean_trace"`.
- I.14 `rationalise.ts`: `rationaliseGolden` asks once through `SweepMeter.rationale` (new phase `rationalise`), content-addressed on `goldenStepsHash(steps)` (`rationaleOf`); unparseable reply stores no rationale but the call is counted. `ExemplarStore` memory + file (`<profile>/exemplars/golden_*.json`); `createGatewayRationale` binds the gateway (temperature 0, integer cents).
- I.15 `repetitive-loop.ts`: `detectRepetitiveLoops(calls, n=3)` over strings or `{name,args}` (canonical JSON args). `trace-writer.ts` sets `AgentTraceRow.failureTags` (new optional field on StorePort.ts, additive; sqlite ALTER column `AgentTrace.failureTags`; contract round-trip on both stores). `gate-score.ts` reads `ActualsOutput.toolInvocations` (falls back to `toolCalls`), zeroes a looping fixture and tags it; `gate.ts` blocks `repetitive_loop` first.
- I.16 `suite-split.ts`: `isPrivateFixture` = explicit `private` flag else `sha256(id)[0] < 77` (~30 percent); overlay `private: [ids]` sets the flag for the whole suite and moves the version. `privateRegressions` blocks `private_regression` before `regression`/`new_failure_cluster`, `verdict.privateRegressions` lists ids. `buildPublicReflectionPrompt` appends the baseline's failing PUBLIC fixtures to the app's reflection prompt; `gepa-pass.ts` uses it.
- Wiring: `status.repetitiveLoops`, `status.repetitiveLoopTraces`, `status.privateRegressions`; the sweep writes a ledger `reject` row with actor `gate:<reason>` for the two new blocks. CLI: status renders the counters; `promote [--live]` distils exemplars into `<profile>/exemplars/` and rationalises with `--live`; the `--live` gateway now also carries `rationalise`.

## Evidence
- `npx vitest run packages/trent-core/src/improve apps/cli/src/commands/__tests__/improve.test.ts` -> exit 0, 16 files, 87 tests (was 12 / 69). Includes the Bun SQLite contract with the `failureTags` round-trip.
- `npx tsc --noEmit -p packages/trent-core/tsconfig.json` -> exit 0; zero errors under `src/improve/`. (Mid-session it showed errors in `src/orchestrator/delegate-port.test.ts` and `apps/cli/src/repl/__tests__/delegate.repl.test.ts`, another agent's files; gone by the final run.)
- No live test added: the new gateway binding is a thin wrapper; nothing in I.13-I.16 needs a real model to prove its claim.

## Notes
- StorePort.ts touched once: `AgentTraceRow.failureTags?: string[]` (optional, additive). Nothing under `src/tools/`, `src/fleet-memory/`, `apps/web/`.
- Existing sweep tests: the retried step no longer appears in the fallback draft's numbered steps or a "Recovery patterns" section; `error_recovery` still fires (triggers are decided on the whole group).
- The hash split can make an unmarked fixture private; tests that need a fixture public mark it `private: false`.
