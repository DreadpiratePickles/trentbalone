# 2026-09-15 — T1.2 heartbeat loop (HEARTBEAT.md, NO_REPLY, quiet hours) + doctor cron path fix

Branch: feature/trent-fleet-v2. Agent task from the backlog plan section T1.2.

## Scope
- `packages/trent-core/src/heartbeat/` (quiet-hours, HeartbeatLoop, default checklist, index)
- config: `HeartbeatConfigSchema` + `heartbeat` default block
- setup writes `<profile>/HEARTBEAT.md`
- doctor cron check reads `<profile>/cron/jobs.json` + `runner.lock` (was `<profile>/cron.json`)
- CLI `trent heartbeat start|status|runs`; `gateway start` starts the loop when enabled
- docs: heartbeat.md (new), doctor.md, configuration.md

## Log
- Read CronRunner (tick/lock/history/deliver), cron CLI group, headless runtime, web quiet-hours
  (no test file exists in apps/web for it; reimplemented in core with its own table), web
  runCompanyHeartbeat, fleet-memory/consolidate.ts (`consolidateMemory` exists: needs gateway,
  improve store, companyId).
- Constraint noticed: `doctor/checks/fix-hints.test.ts` forbids the literal `trent cron` inside
  `checks/cron.ts`; the fix hints avoid that phrase rather than editing a test I do not own.
- RED/GREEN in order: quiet-hours (module not found -> 14 pass), HeartbeatLoop (module not found ->
  12 pass; one test fixed to `advanceTimersByTimeAsync` because the tick awaits consolidation
  before `run`), SetupWizard HEARTBEAT.md (expected false to be true -> pass), doctor cron check
  (7 failing on `cron.json` path -> 78 doctor tests pass), CLI heartbeat group (exit 2 unknown
  command -> 10 pass incl. `gateway start` with heartbeat enabled).
- `consolidateMemory` (T1.4) exists; the CLI binds it lazily (model gateway + improve store on
  first use) and passes it to the loop as `consolidate`.
- Note for the orchestrator: commit 952e9b2 (T2.1, another agent) swept my uncommitted
  `apps/cli/src/commands/groups/servers.ts` heartbeat hunk into its commit, so HEAD's servers.ts
  imports `./heartbeat.js`, which is still untracked here. The tree is only consistent once the
  heartbeat files below are committed.

## Verification (exact commands, exit codes)
- `npx vitest run packages/trent-core/src/heartbeat packages/trent-core/src/setup/SetupWizard.test.ts packages/trent-core/src/doctor/checks apps/cli/src/commands/__tests__/heartbeat.test.ts` -> 0
- `npx vitest run` -> 0 (169 files passed, 2 skipped; 1652 tests passed, 27 skipped)
- `npx tsc --noEmit -p apps/cli/tsconfig.json` -> 0
- `npm --prefix packages/trent-core run build` -> 0
- `node scripts/ci/repo-scan.mjs` -> 0

## Files
new: packages/trent-core/src/heartbeat/{quiet-hours.ts,quiet-hours.test.ts,checklist.ts,fleet-state.ts,HeartbeatLoop.ts,HeartbeatLoop.test.ts,index.ts},
apps/cli/src/commands/groups/heartbeat.ts, apps/cli/src/commands/__tests__/heartbeat.test.ts, docs/heartbeat.md
edited: packages/trent-core/src/{index.ts,config/schema.ts,config/defaults.ts,setup/SetupWizard.ts,setup/SetupWizard.test.ts,doctor/checks/cron.ts,doctor/checks/inspection.test.ts},
apps/cli/src/commands/index.ts, apps/cli/src/commands/groups/servers.ts (already in 952e9b2), docs/doctor.md, docs/configuration.md
