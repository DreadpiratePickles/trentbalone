# P2-10 follow-ups bundle: four small defects recorded during the parity push (2026-09-25)

Opus agent, no subagents, no commits, no model calls. HEAD fb90bcb, branch feature/trent-fleet-v2;
other agents edit the tree concurrently. Source: docs/sessions/2026-09-25-parity-push.md, the
"P2-8 landed", "P2-1 landed", "P2-D landed" and "P2-5a" lines.
Owned: apps/cli/src/commands/groups/usage.ts (+ usage.test.ts), service-daemon.ts (+ service.test.ts),
apps/cli/src/runtime/child-run.ts (+ test), packages/trent-core/src/service/program.ts (+ test),
cron.ts (exports only, no behaviour change), apps/cli/src/tui/** (/exit and /quit cleanup only),
the one `trent usage` doc sentence, this log. Env for every vitest run: TRENT_QUEUE_FALLBACK=disabled,
run from the repo root.

## Items
1. `trent usage` text does not name the P2-8 `estimated` / `unpriced` row flags; totals lack counts.
2. service-daemon.ts refuses pinned cron jobs with its own `refusePinnedModel` and duplicates cron.ts
   wiring (delivery, incident alert, social publish handler); cron.ts honours the pin via `cronJobRun`.
3. child-run.ts `selfProgram` duplicates service/program.ts's program resolver.
4. The TUI's `/exit` and `/quit` call `process.exit(0)` without the REPL's cleanup (writer lock).

## Progress
- Item 1 RED: usage.test.ts new case "counts estimated and unpriced rows in --json, and the text
  names each, one line, only when the period has some" plus the two exact `toEqual` totals now
  carrying `estimatedRows: 0, unpricedRows: 0` (requirement change from this bundle: the JSON always
  carries both counts, like `cachedInputTokens`): 3 failed / 5 passed, each on the missing fields.
- Item 1 GREEN: usage.ts adds `UsageTotals` (SpendTotals + the two counts); the counts come from
  `buildSpendReport` over the flagged rows only, so the window/day key is not re-derived; one text
  line per flag after the period totals, only when non-zero. spend-report.ts (not owned) untouched.
  docs/configuration.md: the "does not name either flag yet" sentence now says what it does. 8/8.
- Item 2 RED: service.test.ts "a pinned job under the daemon runs on its pin (the child-run port is
  called with the pin) instead of being refused": the tick wrote a history row but `runtimeDeps` was
  `[["service", undefined]]`, no runtime built on the pin (the daemon's own `refusePinnedModel`).
- Item 2 GREEN: cron.ts exports `buildCronRunner(ctx, { configManager, config, runtime, manager })`
  (the run via `cronJobRun`, the social publish handler, delivery, the incident alert); `openRunner`
  calls it with its lazy manager (same `??=` semantics, no behaviour change). service-daemon.ts
  imports it with its shared manager and loses `parseDeliverTarget`, `refusePinnedModel`, `Wiring`
  and `cronRunner` (269 -> 207 lines). cron.ts 399 -> 409 lines (over the rulebook's 400 review mark,
  under the bundle's 500 limit; cohesion: it is the one owner of the cron group and its runner wiring).
  service + cron suites 32/32; `tsc -p apps/cli` 0.
- Observation (pre-existing, unchanged, same under `cron start`): a stop signal does not abort an
  in-flight job run, so a pinned child started just before SIGTERM finishes on its own.
- Item 3 RED: child-run.test.ts "[P2-10] a pinned run's child and the service unit start the same
  trent", four process states, each comparing `selfProgram(view, realpath)` with the program prefix
  of `trent service install --dry-run --json` under `setServiceHostForTests` (the real call site) and
  with `resolveServiceProgram(view, realpath).argv`: 2 failed / 10 passed; the symlinked compiled
  binary and the npm bin symlink disagreed (child-run's copy never resolved symlinks).
- Item 3 GREEN: program.ts keeps the one resolver; `resolveServiceProgram` takes an optional
  `ProgramRequester` (operation, who starts it, remedy; default `SERVICE_PROGRAM_REQUESTER`, same
  service wording as before) so a child's refusal still says `run.child` and names the pinned run.
  child-run.ts drops its constants and `SelfProcessView`; `selfProgram(view, realpath = fs.realpathSync)`
  calls the resolver. The two P2-1 selfProgram tests now pass an identity realpath (the default reads
  the disk; their expectations are unchanged). Behaviour changes, both toward the unit's: symlinks are
  resolved, and Bun running a .ts source no longer carries execArgv (Bun needs no loader flags).
  Guards added: the child's no-entry refusal (operation run.child, no "install"); program.test.ts's
  default refusal names service.program. child-run + program 20/20; `tsc -p apps/cli` 0.
- Item 4 finding: the premise was half right. `/exit` and `/quit` did call `process.exit(0)` from the
  key handler, but the TUI never registered as a writer on its profile at all (only the REPL, the
  headless runtime, the gateway, cron and the daemon do), so there was no writer file to release and
  `sessions prune` did not refuse while a TUI session was writing its transcript.
- Item 4 harness first (no behaviour change): `runTui(options)` takes `renderOptions` (Ink's stdin /
  stdout) and a `createOrchestrator` factory, so a test drives it with fake streams and no model.
- Item 4 RED 1: tui/__tests__/exit.test.ts (/exit and /quit): first frame rendered, then
  `liveWriters` was `[]` where a `tui` writer was expected (2 failed).
- Item 4 step A: runTui registers `acquireProfileWriter(profileDir, "tui")` before render and releases
  it in `finally` after `waitUntilExit()`. RED 2: "runTui returning did not happen" for both commands
  (the key handler's `process.exit(0)`, mocked, left the app mounted).
- Item 4 GREEN: App.tsx `/exit` and `/quit` call Ink's `useApp().exit()` and return; runTui's finally
  releases the writer; the binary's existing `process.exit(EXIT.OK)` after `runTui` ends the process.
  Ctrl+C left as is (out of scope): its `process.exit(0)` still removes the writer file through the
  lock module's own `exit` hook (`releaseAllHeld`). exit.test.ts 2/2.
- Doc: docs/configuration.md, the P2-8 paragraph's "the text of `trent usage` does not name either
  flag yet" now says every total counts them (`estimatedRows`, `unpricedRows`) and the text names
  each in one line when the period has some. Real binary on a scratch ledger (tsx, no model):
  `estimated 1 of 3 charges: ...chars/4` and `unpriced  1 of 3 charges: no price row...`, exit 0.

## Verification
Shared tree, mid-session (other agents editing): full vitest set exit 1, 2 failed / 1549 passed,
neither mine: docs-truth "command counts" (the other agent's uncommitted README.md says 34/146; HEAD
says 35/151, which the registry has) and web.test's real-standalone case (30.5 s readiness timeout
under load; alone it passed, served / in 11.2 s, exit 0). `tsc -p apps/cli` exit 2 and core build
exit 2 at that moment, on social/publish.ts, a2a/** and verdict.test.ts (other agents' files).

Isolated: HEAD 68894ea (another agent's commit landed mid-session; it touches none of my files) via
`git archive` into the scratchpad, plus only this bundle's 13 files, node_modules symlinked and the
gitignored generated Prisma client copied in:
- `npx vitest run apps/cli/src/commands/__tests__ apps/cli/src/runtime apps/cli/src/tui apps/cli/src/repl packages/trent-core/src/service packages/trent-core/src/cron packages/trent-core/src/wrapped-modules.test.ts`: exit 0, 94 files, 1544 passed, 1 skipped (web.test's real-standalone case: no apps/web build in the copy).
- `npx tsc --noEmit -p apps/cli/tsconfig.json`: exit 0.
- `cd packages/trent-core && npm run build`: exit 0.
- `node scripts/ci/repo-scan.mjs`: exit 0.
Shared tree at the end: repo-scan exit 0; `tsc -p apps/cli` exit 0; owned suites (usage, service,
cron, child-run, service/, tui/) 13 files, 123 tests, exit 0.

## Files changed (no commit)
apps/cli/src/commands/groups/usage.ts, apps/cli/src/commands/__tests__/usage.test.ts,
apps/cli/src/commands/groups/service-daemon.ts, apps/cli/src/commands/__tests__/service.test.ts,
apps/cli/src/commands/groups/cron.ts (export only), apps/cli/src/runtime/child-run.ts,
apps/cli/src/runtime/child-run.test.ts, packages/trent-core/src/service/program.ts,
packages/trent-core/src/service/program.test.ts, apps/cli/src/tui/App.tsx, apps/cli/src/tui/index.ts,
apps/cli/src/tui/__tests__/exit.test.ts (new), docs/configuration.md (one sentence), this log (new).
Largest: service.test.ts 469, cron.ts 409; all under 500.
- Shared tree, final: `cd packages/trent-core && npm run build` exit 0; the full vitest set exit 1,
  6 failed / 1550 passed, none in this bundle's files: run-verdict.test.ts (untracked, P2-11's work
  in progress, 5 cases) and docs-truth "toolsets" (README says sixteen names; the schema now has 17
  with P2-9's a2a toolset; README belongs to another agent).
