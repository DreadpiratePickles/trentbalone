# 2026-09-25, P2-B: README rewrite and text-mode run output

Agent: Opus subagent (no sub-subagents, no commits, no model calls). Branch `feature/trent-fleet-v2`,
HEAD `fb90bcb`; six other agents edit the tree concurrently. Env for every vitest run:
`TRENT_QUEUE_FALLBACK=disabled`, run from the repo root.

Owned: `README.md`, `apps/cli/src/commands/groups/run.ts` and `apps/cli/src/commands/__tests__/run.test.ts`
(text-mode console handling only), `docs-truth.test.ts` (README into TOOLSET_PAGES only),
`05_release/output/README-draft-2026-09-25.md` (superseded marker), this log. The saved transcript
file is NOT edited (it stays the verbatim capture); the README's copy drops its `[Worker]` lines.

## Inputs read
- Rulebook; README draft (audited, proof comments); public-readiness audit; the keyed first-run
  transcript (2 cents, 5 ledger rows); scorecard sections 3 and 4; parity-push log (the "For the
  README owner" lines of P2-A1, P2-A2, P2-D, P2-5a, P2-F, P2-8, P1-A, P2-3, P2-1, P2-7, P2-6);
  docs/sessions/2026-09-25-p2-5a-truth.md; docs/README.md; the current README.
- Found at start: the working-tree README says `34 commands, 146` while HEAD says `35 commands,
  151` (an uncommitted edit by another agent or a revert); the registry decides, checked below.

## Log
- Text mode (item 1). Requirement change, recorded here before the code: text-mode `trent run` no
  longer leaves the app's console alone; the old test "text mode leaves the console alone" encoded
  the previous requirement and is replaced by a describe block for the new one (the --json and
  stream-json tests are untouched). Design: pino 10.3.1 (`node_modules/pino/lib/tools.js:362-368`)
  writes through `process.stdout` instead of straight to fd 1 when `process.stdout.write` is not the
  stream's own method at logger build time, so routing `process.stdout.write` (passing the run's own
  `ctx.out` lines through) plus console.log/info/debug covers both the `[Worker]` lines and the
  pino lines, and the level is left alone so the lines are kept, not silenced. File:
  `<profile>/logs/run.log`, append, 1 MiB cap with one `.1` generation (the policy of
  `service/service-log.ts`, whose `ServiceLog` hard-codes a `service[pid]` prefix, so not reused).
  `--verbose` keeps the old behaviour.
- RED: `npx vitest run apps/cli/src/commands/__tests__/run.test.ts` -> exit 1, 5 failed / 22 passed
  (pino line reached the terminal; no run.log; the cap constant missing; `--verbose` unknown option,
  exit 2; `--help` has no `--verbose`).
- Size rule: run.test.ts would have been 593 lines with the new block, so the block moved to a new
  `apps/cli/src/commands/__tests__/run-text-log.test.ts` (163 lines, compact fake runtime) and
  run.test.ts only loses the obsolete test (469 lines). run.ts 497 lines. The moved tests re-proved
  red against HEAD's run.ts (swapped in for one command, restored, `cmp` clean): 5 failed / 2 passed
  (the two passing: `--json` writes no run.log; console and stdout.write restored).
- GREEN: `npx vitest run apps/cli/src/commands/__tests__/run-text-log.test.ts .../run.test.ts
  .../run-model.test.ts` -> exit 0, 3 files, 32 passed.
- Real app, keyless, throwaway HOME/TRENT_HOME under the scratchpad, `env -i` (no runtime path loads
  `.env.local`; grep for loadEnvConfig/dotenv on the run path found none): under Node
  (`npm run cli`) `run "Write a one-line tagline for a bakery"` -> exit 1, stdout is the run's own
  lines only, stderr empty, `<profile>/logs/run.log` (0600) holds a header and the five `[Worker]`
  lines. Same under Bun (`npm run cli:bun`). Keyless runs log no pino line at all (0 with
  `--verbose` too), so the pino route is proved by a probe instead: tampered `process.stdout.write`,
  then `pino({level:"debug"}).debug(...)` -> captured through the tampered write under node and
  bun (both exit 0).
- README facts, keyless throwaway profiles under the scratchpad (`env -i`, HOME=TRENT_HOME=scratch):
  `npx tsx apps/cli/src/index.ts --help` exit 0 (35 commands listed plus Commander's `help`);
  docs-truth on the old README: 11 passed / 1 failed, `expected [ 34, 146 ] to deeply equal [ 35, 151 ]`
  (so the working-tree "34/146" edit was wrong; the registry is 35/151). `fleet packs` exit 0
  (14 packs; the three market packs' state lines now name the real calls); `fleet install
  small-business` 0; `fleet show finance` 0; `budget status` 0; `fleet list --json` 0 (173 agents,
  9 seats); `doctor` exit 3 (`total 22 passed 12 warnings 3 failed 2 skipped 5`: no key, Docker daemon
  stopped); `service --help`, `run --help`, `usage --help` 0; bare `trent` with `/exit` piped after
  10 s: `Setup did not complete: No provider key found ...`, the boot, the DEGRADED paragraph,
  `Session ended.`, exit 0, no config.yaml written.
- Test counts: parity-push records CI greens but no counts, so the counts come from the CI run it
  names last: run 36198887419 on 68894ea (2026-09-25T22:55Z, all-checks-pass success), job logs via
  `gh run view --job <id> --log`: core tests 3953 passed | 23 skipped (3976), 404 files; web tests
  2829 passed | 125 skipped (2955), 515 passed | 13 skipped files (528); sandbox/docker suites 231
  passed, 33 files; four binaries built and run on their OS.
- Wall times re-measured on a keyless profile: `--version` 7.93 s, `fleet install` 20.32 s, keyless
  run 45.49 s, against the audit's 1.68 s / 1.43 s / 3.78 s: the machine is loaded by six agents'
  test runs, so the README claims no per-command time except the keyed run's own recorded 15035 ms
  and P2-A1's recorded `npm install` (12 s with the postinstall).
- Release state probed (read-only): installer URL 404; `gh release list` empty; `git ls-remote
  --tags` 0; default branch `main`, license null, `repos/.../readme` 404. `improve status --json`
  on one profile: Node `durable:false` ("needs Bun ... `npm run cli:bun`"), Bun `durable:true` and
  `trent.db` present.
- README written from the draft (generated from a template in the scratchpad; the keyed transcript
  block is lines 5-38 of the saved capture with its four `[Worker]` lines dropped, nothing else
  changed). 319 lines, 34 proof comments. The one em-dash left is inside the model's own draft in
  the verbatim transcript. Draft marked SUPERSEDED on its first line.
- docs-truth: README added to TOOLSET_PAGES (the comment above it asked for this once README was
  corrected). Real test in a HEAD export (`git archive HEAD` of 68894ea into the scratchpad, my five
  files overlaid, dependencies linked, `@trent/core` pointed at the export): 12 passed, exit 0.
  In the shared working tree: 11 passed / 1 failed, `README.md: "accepts sixteen names": expected
  16 to be 17`, because P2-9 (A2A client, in flight, unlanded) adds a 17th toolset `a2a`.
  README describes landed code, so it keeps sixteen and "A2A has no client".

## Verification
- `npx tsc --noEmit -p apps/cli/tsconfig.json` -> exit 0.
- `node scripts/ci/repo-scan.mjs` -> exit 0 (1073 files; canned, hex, emoji all PASS; it does not
  scan README).
- `npx vitest run apps/cli/src/commands/__tests__ packages/trent-core/src/wrapped-modules.test.ts`
  in the working tree -> exit 1, 44 files, 1157 passed / 6 failed: 5 in P2-11's new
  `run-verdict.test.ts` (its RED tests, run.ts not yet edited by P2-11) and the README toolset count
  against P2-9's unlanded schema.
- Same command in the HEAD export with my files -> exit 1, 1154 passed / 1 failed / 1 skipped: the
  ACP stdio subprocess test timed out waiting 30 s for a first frame under load; alone
  (`npx vitest run apps/cli/src/commands/__tests__/servers.test.ts`) -> exit 0, 6 passed.

## For the lead
- When P2-9 lands, README.md needs (docs-truth enforces the first): the toolsets sentence ->
  "accepts seventeen names: ... `social`, `business` and `a2a`." and "gets nine of them: everything
  except `browser`, `vision`, `memory`, `mcp`, `media`, `social`, `business` and `a2a`." (check what
  QuickSetup now writes for `a2a`); a Tools row `a2a` | `a2a_list`, `a2a_discover`, `a2a_send`,
  `a2a_history` | Opt-in; configured peers only; every send asks; the Hermes table A2A row -> server
  and client; the not-done A2A line loses "no client".
- run.ts is 497 lines and P2-11 still has to add its exit-mapping hunks there; it will pass 500.
  Suggested split: move `quietStdoutForMachines`, `RUN_LOG_MAX_BYTES`, `appendRunLog` and
  `routeAppOutputToLog` into a `groups/run-output.ts`. Not done here to avoid racing P2-11's edit.
- docs/README.md does not index docs/service.md (P2-D added the page after the index was written).
- Not verified live: the pino route on a keyed run (keyless runs log no pino line; the mechanism is
  proved by the probe and the unit test).

## Files
README.md; apps/cli/src/commands/groups/run.ts; apps/cli/src/commands/__tests__/run.test.ts (the
obsolete test removed); apps/cli/src/commands/__tests__/run-text-log.test.ts (new);
apps/cli/src/commands/__tests__/docs-truth.test.ts (TOOLSET_PAGES + its comment);
05_release/output/README-draft-2026-09-25.md (superseded marker); this log.
